import type { RoutableProps } from "preact-router";
import { CorpusSectionIndex } from "@/pages/CorpusSectionIndex";

export function PlatformsIndex(_: RoutableProps) {
  return <CorpusSectionIndex kind="platforms" />;
}
