# Shared path rules for the develop PR CI classifier.
#
# The classifier fails closed: every changed path must match `safe-content`
# before Python code CI can be skipped. Any non-matching path, including a new
# top-level area, is treated as `code-ci`. `safe-content` lists prose-only
# extensions, not directories. Directories like `docs/` and `_project/` mix
# prose with executable code (Sphinx config, extension modules, helper
# scripts) — those code paths must run lint/type/test like any other code.
safe-content:
  - "_project/blind-spots/**"
  - "_project/decisions/**"
  - "_project/handoffs/**"
  - "_project/notes/**"
  - "_project/research/**"
  - "_project/planning/**"
  # Deterministic machine-generated tracker export snapshot (written weekly by
  # .github/workflows/todo-db-export.yml). Data, not authored prose, so it is
  # deliberately safe-content but NOT content-guard: routing generated files
  # through markdownlint --fix would let a future generated table stick the
  # weekly auto-merge PR with no author to fix it.
  - "_project/todo-db-export/**"
  - "_blog/**.md"
  - "_blog/**.rst"
  - "_blog/**.png"
  - "_blog/**.jpg"
  - "_blog/**.jpeg"
  - "_blog/**.gif"
  - "_blog/**.svg"
  - "_blog/**.json"
  - "docs/**.md"
  - "docs/**.rst"
  - "docs/**.txt"
  - "*.md"
  - "CLAUDE.md"
  - "AGENTS.md"

# Documentation-only changes are validated by the content guard. The guard
# patterns intentionally stay broader than `safe-content` so a docs change
# that touches both prose and Sphinx code still triggers docs-validate
# alongside the full PR tier.
content-guard:
  - "_project/blind-spots/**"
  - "_project/decisions/**"
  - "_project/handoffs/**"
  - "_project/notes/**"
  - "_project/research/**"
  - "_project/planning/**"
  - "_blog/**"
  - "docs/**"
  - "*.md"
  - "CLAUDE.md"
  - "AGENTS.md"

# Explicit code paths. Unknown paths also fall through to `code-ci` because
# the classifier fails closed when no `safe-content` pattern matches. The
# `docs/**.{py,js,css,html,jinja,j2}` and explicit `docs/conf.py` /
# `docs/Makefile` / `docs/_extensions/**` / `docs/_static/**` /
# `docs/_templates/**` patterns route the Sphinx machinery (which is
# executable Python and templates, not prose) through full lint/type/test.
code-ci:
  - "benchbox/**"
  - "tests/**"
  - "scripts/**"
  - "examples/**"
  - "landing/**"
  - "results-data/**"
  - "results-explorer/**"
  - "docker/**"
  - "_binaries/**"
  - "_sources/**"
  - "_project/scripts/**"
  - "_project/evals/agent-instructions/**"
  - "_project/config/**"
  - "_project/compat/**"
  - "docs/conf.py"
  - "docs/Makefile"
  - "docs/_extensions/**"
  - "docs/_static/**"
  - "docs/_templates/**"
  - "docs/**.py"
  - "docs/**.js"
  - "docs/**.css"
  - "docs/**.html"
  - "docs/**.jinja"
  - "docs/**.j2"
  - "docs/development/agent-*.md"
  - "docs/operations/agent-instruction*.md"
  - "pyproject.toml"
  - "uv.lock"
  - "Makefile"
  - ".github/workflows/**"
  - ".pre-commit-config.yaml"
  - ".claude/commands/**"
  - ".claude/settings.json"
  - ".claude/skills/**"
  - "CLAUDE.md"
  - "GEMINI.md"
  - "ANTIGRAVITY.md"
  - "AGENTS.md"
  - "skill-sync.yaml"
  - "skill-sync.lock"
  - ".gitignore"
  - ".gitattributes"

# Extra path-filter groups (beyond the three core buckets above). Each key
# here becomes a `<group>-needed` boolean output from ci-paths, computed by
# path_filter_decision.py, so a pr.yml job can gate on
# `needs.ci-paths.outputs.<group>-needed == 'true'` without inventing a
# second classifier. Add a new group by adding a key here — no script
# changes required.

# Packaging-metadata changes: build/install/audit signal (package-smoke,
# dependency-audit jobs in pr.yml) only needs to run when one of these
# changed. See tracker item `pr-gate-package-and-audit-promotion`.
packaging:
  - "pyproject.toml"
  - "MANIFEST.in"
  - "setup.py"
  - "setup.cfg"
  - "benchbox/__init__.py"
  - "uv.lock"

# Results-explorer visualization / CLI<->explorer parity source changes:
# gates the parity-check job in pr.yml. See
# tracker item `parity-check-develop-ring`.
viz:
  - "results-explorer/**"
  - "tests/parity/**"
  - "_project/scripts/explorer_pipeline/**"

# Results Explorer unit-test contract. Keep this broader than the frontend
# source tree: read-model/build-contract, curated data, fixture generators,
# and publication workflow changes can all invalidate Vitest expectations.
explorer-vitest:
  - "results-explorer/**"
  - "_project/scripts/explorer_pipeline/**"
  - "_project/scripts/explorer_publish.py"
  - "results-data/**"
  - "scripts/generate_corpus_inventory.py"
  - "tests/parity/fixtures/**"
  - "tests/unit/core/tuning/fixtures/**"
  - ".github/workflows/results-explorer-browser.yml"
