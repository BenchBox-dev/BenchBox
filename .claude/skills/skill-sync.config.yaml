# Generated by skill-sync. Do not edit manually.
# Re-run `skill-sync sync` to regenerate.
code:
  fast_test: uv run -- python -m pytest -m fast -q
  format: uv run ruff format .
  line_length: 120
  lint: uv run ruff check .
  lint_fix: uv run ruff check --fix .
  typecheck: uv run ty check
  verify: uv run ruff check . && uv run ty check && uv run -- python -m pytest -m
    fast -q
test:
  coverage: uv run -- python -m pytest --cov=benchbox --cov-report=term-missing
  runner: pytest
todo:
  cli: uv run _project/scripts/todo_cli.py
  index: uv run _project/scripts/generate_indexes.py
  validate: uv run _project/scripts/validate_todo.py
