# Antigravity adapter

Read and follow `AGENTS.md`; it is the active BenchBox authority. Load relevant
generated skills from `.agents/skills/`. Keep the shared mirror at that path; `.antigravity/skills/` is no longer populated.
