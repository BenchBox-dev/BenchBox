{
  "description": "Platforms like Polars, Pandas, DuckDB, and others support both SQL and DataFrame\nexecution modes. The CLI exposes this via `--mode sql|dataframe`, but the\ninteractive wizard always uses the platform's default mode.\n\nUsers should be able to choose their execution mode to:\n1. Compare SQL vs DataFrame API performance\n2. Test DataFrame-native query implementations\n3. Use the mode that matches their production workload",
  "legacy": {
    "item_meta": {
      "category": "User Experience",
      "created_at": "2025-12-21T00:00:00Z",
      "worktree": "ux-improvements"
    }
  }
}
