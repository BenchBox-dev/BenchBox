{
  "acceptance": [
    "Each applied fix checked with its narrowest proving check (e.g. pytest tests/unit/core/results/..., pytest tests/unit/scripts/explorer_pipeline/..., or docs/link check); pass/fail reported per fix",
    "Only ACCEPT/NARROW small file-scoped items touched; design-decision items (read-model v11, provenance cutoff) moved to follow-on TODOs, not fixed inline",
    "Nothing committed unless user separately asks; explicit-path staging (no git add -A) if edits staged",
    "Work done in a write worktree (never primary clone) per worktree policy",
    "No fix reintroduces raw version-key reads or breaks legacy-bundle fallback; each fix verified compatible with the post-#2199 shape (helper + renamed REQUIRED_TOP_KEYS)"
  ],
  "context": "Claim only after reconcile-review is done and its audit section names the ACCEPT/NARROW set. Candidates from draft (confirm against reconciliation, do not assume): delete-or-fix orphaned scripts/validate_corpus.py (P2); add Tables Block to docs/reference/result-formats.md + document databricks_clustering_strategy (P7); backfill CHANGELOG.md entries for #2178/#2177/#2122/#2030 (P7); extend benchbox/validation/bundle.py checks if accepted (P1). PR #2199 assumed merged: every edit must use result_schema_version_value() (never add a new raw data.get(version) version check), respect the renamed REQUIRED_TOP_KEYS with legacy-version fallback preserved, keep legacy version inside OPTIONAL_KEYS/tuning-envelope sets, and preserve loader/validator/explorer acceptance of old bundles. Design-decision items stay out: read-model v11 for per-table load time, provenance cutoff scheme for pre-fix Databricks bundles (note export.benchbox_version as candidate mechanism, do not design it here). Authority: shared-change-framework/SKILL.md; user phrasing accept-or-rebut+integrate authorizes these fixes without a second approval round (review-response.md).",
  "description": "Goal: apply direct small file-scoped edits for every ACCEPT/NARROW item whose remediation needs no user design decision. Method per shared-change-framework/SKILL.md (explicit-path staging, no git add -A): edit in a write worktree created via make worktree-create + make agent-write-preflight, run narrowest proving check per file, report pass/fail per fix. Candidates (verify against reconciliation first): orphaned scripts/validate_corpus.py delete-or-fix (P2); Tables Block section in docs/reference/result-formats.md + databricks_clustering_strategy docs (P7); CHANGELOG.md backfills (P7); benchbox/validation/bundle.py check extension if accepted (P1). Post-#2199 compatibility is mandatory: route every version-key read through result_schema_version_value(), honor the renamed REQUIRED_TOP_KEYS while keeping legacy-bundle acceptance, and do not reintroduce bare version checks in schema.py/loader.py/bundle.py/transformer.py/localResult.ts. Out of scope inline: anything needing an undecided design (read-model v11 per-table load time, Databricks provenance cutoff, #2199 fallback-matrix/docs follow-ons) — moves to follow-on TODOs. Nothing committed unless user separately asks.",
  "links": [
    "_project/audits/result-bundle-downstream-compat-2026-09-18.md"
  ]
}
