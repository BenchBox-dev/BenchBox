{
  "acceptance": [
    "One TODO per DEFER item and per larger follow-on from reconciliation (e.g. Explorer read-model v11 scoping for per-table load time, Databricks provenance/cutoff marker, shared Python/TS bundle-field contract test for P5/P6)",
    "needs/dependency links recorded where one genuinely blocks another (e.g. decide whether per-table load time is surfaced before implement read-model v11 change)",
    "Each TODO records acceptance criteria + links to audit doc and specific finding it closes",
    "Dependency edges resolve to real sibling TODOs (verified via show_item), no dangling refs"
  ],
  "context": "Claim after reconcile-review (and integrate-fixes triage) so the DEFER set is known. Examples are candidates only — derive the real set from the reconciliation table. Sequence with needs links only where one genuinely blocks another. Authority: todo skill + mcp__todo-db tools.",
  "description": "Goal: turn every DEFER item and larger follow-on from reconciliation into its own sequenced TODO. Method with todo skill / mcp__todo-db tools: create one TODO per DEFER item and per larger follow-on identified during reconciliation (e.g. scoping Explorer read-model v11 for per-table load time, corpus provenance/cutoff marker for Databricks clustering fix, shared Python/TS bundle-field contract test closing P5/P6). Sequence with needs links only where one genuinely blocks another (e.g. decide whether per-table load time should be surfaced before implement read-model v11 change). Record acceptance criteria and links back to _project/audits/result-bundle-downstream-compat-2026-09-18.md and the specific finding each TODO closes.",
  "links": [
    "_project/audits/result-bundle-downstream-compat-2026-09-18.md"
  ]
}
