{
  "acceptance": [
    "ClickHouse native Delta read path exists (deltaLake table function / DeltaLake engine SQL builders)",
    "Unit tests cover SQL generation and identifier/literal handling without a server",
    "Docker-gated live test proves SELECT against a real ClickHouse server and real Delta table",
    "False 'cannot read directly' statements corrected to describe native path and conversion fallback"
  ],
  "context": "Source TODO clickhouse-delta-support-via-conversion-or-external-tooling assumed ClickHouse lacks native Delta support. Prior verification against ClickHouse docs showed native reads exist: DeltaLake table engine and deltaLake/deltaLakeS3/deltaLakeAzure/deltaLakeLocal table functions. CLOSED COMPLETE: builders, probe, and doc corrections landed in PR 2269 (merged as 1f1d059d0); the deferred acceptance item (live SELECT against real data) was completed in PR 2290 — Docker-gated tests passing on clickhouse-server 25.8 (server 25.8.33.6), including deltaLakeLocal row reads and public-S3 reads through both the table function and the engine. Adapter selection wiring also in PR 2290 (open, merge pending maintainer review).",
  "description": "Implement actual ClickHouse native Delta Lake reads to supersede the conversion-only approach. Add SQL builders for the deltaLake table function family and DeltaLake engine, unit tests, a Docker-gated live SELECT test, and correct the inaccurate no-native-support statements."
}
