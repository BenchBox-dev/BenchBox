{
  "acceptance": [
    "D1 criteria were confirmed with the maintainer and recorded in this item before the replay ran.",
    "The replay harness and a report under _project/analysis/ list, per merge: parent SHA, selected tests, fallback reason if any, and estimated runner-minutes and wall time.",
    "The report states recall on known regressions and on attributable canary failures since 2026-09-14, with every miss explained.",
    "The maintainer's D1 go or no-go decision is recorded in this item; on no-go, a follow-up item names what must change."
  ],
  "context": "Pre-registered D1 criteria. Confirm or change them with the maintainer BEFORE running the replay, record the confirmed values in this item, and do not change them after seeing results:\n- Recall: 100% on the known-regression commits and on every attributable canary failure since 2026-09-14. Any miss is a no-go until the selector is fixed and the replay is rerun.\n- Cost: mean estimated runner-minutes per merge at most 40 (the full suite is about 100), and p90 wall time at most 30 minutes.\n- Whole-suite fallback rate reported. Above 30% of merges, the savings case is weak; report it for the maintainer rather than tuning the fallback away.\n\nGuardrails:\n1. Read-only against the repository: use temporary worktrees or git archive; never modify develop or push.\n2. Report every miss with the merge, the failing test, and the missing edge. Do not tune thresholds, exclude merges, or weaken fail-safe rules to pass.\n3. Commit the harness and a durable report under _project/analysis/ with the exact merge SHAs used, so the numbers can be replayed.\n4. If a failure cannot be attributed to one merge, record it as unattributed with the evidence; do not count it as a hit.",
  "description": "Measure the selector against history before any CI wiring. Validation must use the dependency map as it existed before each merge: a map built from today's tree says nothing about what would have been selected then.\n\nBuild a replay harness that, for each of about the last 60 develop merges (and every known-regression commit), checks out the merge's parent tree, builds the selector map there, applies that merge's exact diff, and records: selected node IDs, whether a whole-suite fallback fired and why, and estimated runner-minutes and wall time from measured per-test durations plus per-shard setup (from canary-impact-01-junit-durations), using the selector's shard sizing (about 10 minutes of tests per shard, 1 to 6 shards).\n\nAlso replay every daily canary failure since 2026-09-14: for each failing test, find the merge that introduced the failure and check the selector selected that test on that merge.\n\nDECISION POINT D1 (maintainer): go or no-go for the shadow job (canary-impact-04-shadow-job). The agent reports the numbers and stops; it does not start item 04."
}
