{
  "acceptance": [
    "load_customer_scd2_batch commits DuckDB customer rows; DimCustomer is non-empty after a historical DuckDB ETL run.",
    "SCD2 versioning and rollback unit tests run on both sqlite3 and DuckDB and fail without the fix.",
    "tests/integration/test_tpcdi_end_to_end.py passes under the canary marker expression.",
    "The fix is merged to develop."
  ],
  "context": "Found by release-canary run 35993930883 on develop 1ba01a835: shards 2-6 failed, all 9 failures in test_tpcdi_end_to_end.py with zero customers loaded. Fixed by PR #2329, merged 2026-09-24T14:11:45Z as 4f30755528e4c07d0f44a6d26c7887631d38ea19 on develop. make pr-preflight passed at the PR head; 34 canary-marked and 368 default-marked TPC-DI tests passed locally. Canary re-verification is tracked by canary-green-for-v0-4-1.",
  "description": "SQLETLBackend.load_customer_scd2_batch (added by #2316) ran BEGIN and the DimCustomer inserts on connection.cursor(), then committed the parent connection. On DuckDB, cursor() is a separate connection with its own transaction, so the inserts were never committed and DimCustomer stayed empty while the load reported success. Every tests/integration/test_tpcdi_end_to_end.py DuckDB test fails (including test_complete_etl_workflow_sqlite, whose fixture is a DuckDB file). Fix: run BEGIN, writes, and COMMIT/ROLLBACK on one handle; parametrize the SCD2 unit tests over sqlite3 and DuckDB.",
  "links": [
    "benchbox/core/tpcdi/etl/sql_backend.py",
    "tests/unit/core/tpcdi/test_benchmark_flow_coverage.py",
    "tests/integration/test_tpcdi_end_to_end.py",
    "https://github.com/BenchBox-dev/BenchBox/actions/runs/35993930883",
    "https://github.com/BenchBox-dev/BenchBox/pull/2316"
  ]
}
