{
  "context": "Decide the seam before writing the adoption, not during. The shape that satisfies both rules: the RunRequest carries the profile NAME, and the core run service resolves connection policy through a surface-supplied resolver it invokes at adapter-construction time -- MCP passes one that reads BENCHBOX_MCP_CLICKHOUSE_PROFILES, CLI passes one that resolves from its own config or refuses. Policy stays surface-scoped, the execution path is shared, and resolution stays late. Port test_clickhouse_profile_withdrawn_by_the_operator_fails_closed to the new call site BEFORE deleting the translator, and prove the ported test still fails when resolution is hoisted -- a ported test that passes under a hoisted implementation has stopped testing the property.",
  "description": "one-engine-mcp-run-service-adoption's rung 1 requires _prepare_adapter_platform_options to be gone from benchbox/mcp/tools/benchmark.py, and its anti-patterns say option translation belongs below both surfaces while security policy stays at the MCP layer. Those two rules collide on one line of that function, and the collision is security-relevant.\n\n_prepare_adapter_platform_options is not purely translation. For ClickHouse it calls resolve_clickhouse_connection_profile(name) -> {'port', 'secure'}, reading the operator-owned BENCHBOX_MCP_CLICKHOUSE_PROFILES registry. It does that deliberately at EXECUTION time, not admission, so only the profile NAME is ever persisted in a durable job and a queued retry re-resolves against current policy -- withdrawing a profile fails closed for work already in the queue. That is the documented contract in docs/operations/mcp-remote-security.md, 'Server-owned ClickHouse connection profiles'.\n\nThe adoption item's own approach text already picks the risky side: 'ClickHouse profile resolution stays MCP-side ... but its OUTPUT feeds the service's standard option path'. If that output is computed when the RunRequest is built, the resolved destination is what gets persisted, and the withdrawal guarantee is gone.\n\nASYMMETRIC TEST COVER, which is the real risk. The persistence half is pinned by tests/integration/mcp/test_durable_jobs.py:196, which asserts persisted.request['platform_options'] == {'connection_profile': 'reviewed'} -- hoisting resolution to admission breaks that test loudly. The execution-time half is pinned by tests/unit/mcp/test_benchmark_tools.py:364 test_clickhouse_profile_withdrawn_by_the_operator_fails_closed, which drives _prepare_adapter_platform_options DIRECTLY -- the very function rung 1 mandates deleting. Whoever does the adoption must rewrite that test, and a rewrite aimed at the new call site without preserving WHEN resolution happens loses the guarantee with no failing test to show for it.\n\nRaised 2026-08-04 from the mcp-v2-platform-options side (#1510 built this boundary) after the one-engine coordination brief.\n\nSUPERSEDES `one-engine-clickhouse-profile-resolution-seam` and its two predecessors, all dropped the same day while fitting this item to the tracker's schema, its lint gate, and finally a rung selector that matched no tests. `drop` is a soft state change, not a delete, so no id could be reused; each tombstone carries its reason.",
  "legacy": {
    "anti_patterns": [
      {
        "dont": "materialize port/secure into the RunRequest at admission so the service sees a plain option dict",
        "instead": "persist the profile name and resolve through a surface-supplied resolver at adapter-construction time",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "why": "the resolved destination then lands in durable job storage, and a retry replays a destination the operator has since withdrawn -- the exact failure the profile design exists to prevent"
      },
      {
        "dont": "move the BENCHBOX_MCP_CLICKHOUSE_PROFILES registry into benchbox.core with the rest of the translation",
        "instead": "keep the registry at the MCP surface and inject only the resolver",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "why": "it pushes an MCP-scoped security policy below the surface boundary and hands CLI runs an env registry that was never reviewed for them, contradicting the ADR's own scoping rule"
      },
      {
        "dont": "port test_clickhouse_profile_withdrawn_by_the_operator_fails_closed by pointing it at the new call site and confirming it goes green",
        "instead": "port it, then temporarily hoist resolution to admission and confirm the ported test FAILS before keeping it",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "why": "green proves the new call site exists, not that resolution is still late; the test passes under a hoisted implementation too, so porting it that way silently deletes the guarantee"
      }
    ],
    "item_meta": {
      "category": "MCP",
      "completed_at": "2026-08-09T02:22:42Z",
      "created_at": "2026-08-04T23:55:48Z",
      "worktree": "one-engine"
    },
    "preserves": [
      {
        "behavior": "A profile withdrawn by the operator fails closed for work already queued, not only for new admissions",
        "item_id": "one-engine-clickhouse-profile-late-binding"
      },
      {
        "behavior": "Durable jobs persist only the profile name; the resolved port/secure tuple is never written to job storage",
        "item_id": "one-engine-clickhouse-profile-late-binding"
      },
      {
        "behavior": "Rejections still never echo the requested profile name or enumerate configured profiles",
        "item_id": "one-engine-clickhouse-profile-late-binding"
      },
      {
        "behavior": "benchbox.mcp still never imports benchbox.cli, and the profile registry stays MCP-scoped rather than becoming a core concern",
        "item_id": "one-engine-clickhouse-profile-late-binding"
      }
    ],
    "prior_art": [
      {
        "concept": "the injected-factory pattern that keeps benchbox.core free of benchbox.platforms is the precedent for a surface-supplied connection resolver",
        "decision": "reuse",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "path": "benchbox/core/"
      },
      {
        "concept": "execution-time profile resolution (#1510); the timing must survive the run-service move even though the function will not",
        "decision": "supersede",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "path": "benchbox/mcp/tools/benchmark.py:98-110"
      },
      {
        "concept": "pins that only the profile name is persisted; it is the loud half of the cover and must stay green throughout",
        "decision": "reuse",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "path": "tests/integration/mcp/test_durable_jobs.py:196"
      },
      {
        "concept": "pins execution-time re-resolution but drives the function slated for deletion; port it to the new seam and re-prove it fails when resolution is hoisted",
        "decision": "extend",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "path": "tests/unit/mcp/test_benchmark_tools.py:364"
      }
    ],
    "scope_rules": [
      {
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "kind": "do_not_modify",
        "path_glob": "benchbox/cli/"
      },
      {
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "kind": "do_not_modify",
        "path_glob": "benchbox/platforms/"
      },
      {
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "kind": "only_modify",
        "path_glob": "benchbox/core/run_service.py"
      },
      {
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "kind": "only_modify",
        "path_glob": "benchbox/mcp/**"
      },
      {
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "kind": "only_modify",
        "path_glob": "docs/operations/mcp-remote-security.md"
      },
      {
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "kind": "only_modify",
        "path_glob": "tests/**"
      }
    ],
    "verifications": [
      {
        "command": "PYTHONPATH=. uv run -- pytest tests/integration/mcp/test_durable_jobs.py -q -n 0 -k never_persist_a_connection_tuple",
        "description": "Only the profile name is persisted in a durable job",
        "expected": "exits 0 today (1 selected, 1 passed -- verified 2026-08-04). A failure here means the destination was materialized at admission. Note the selector must name the test, not the word 'profile': the test is test_durable_clickhouse_requests_never_persist_a_connection_tuple, and '-k profile' selects nothing and exits 5, which would make this rung vacuous.",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "last_result": null,
        "last_run": null,
        "seq": 1,
        "workspace_fingerprint": null
      },
      {
        "command": "PYTHONPATH=. uv run -- pytest tests/unit/mcp -q -n 0 -k withdrawn",
        "description": "A withdrawn profile still fails closed on replay after the translator moves",
        "expected": "exits 0 today and must keep exiting 0, but the rung only counts when it also fails on a deliberately hoisted implementation: hoist resolution to admission, confirm this exits non-zero pre-fix, then restore. A ported test that stays green under hoisting has stopped testing the property.",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "last_result": null,
        "last_run": null,
        "seq": 2,
        "workspace_fingerprint": null
      }
    ],
    "work_needs": [
      {
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "needs_wid": "w0",
        "wid": "w1"
      },
      {
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "needs_wid": "w1",
        "wid": "w2"
      }
    ],
    "work_units": [
      {
        "evidence": "w0: confirmed two-halves at HEAD: MCP benchmark.py still has _prepare_adapter_platform_options and CLICKHOUSE_PROFILES registry (admission), core run_service.py has no clickhouse logic (execution). Pins: test_durable_clickhouse_requests_never_persist_a_connection_tuple pins 'only name persisted' (1 passed), test_clickhouse_profile_withdrawn_by_the_operator_fails_closed pins 'withdrawn fails closed on replay' (1 passed in unit/mcp -k withdrawn)",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "notes": null,
        "started_at": "2026-08-09T02:13:51Z",
        "started_branch": "develop",
        "started_worktree": "/Users/joe/Developer/BenchBox",
        "status": "done",
        "summary": "Confirm the two-halves cover at HEAD and write down which test pins which property",
        "wid": "w0"
      },
      {
        "evidence": "w1: decision recorded: keep BENCHBOX_MCP_CLICKHOUSE_PROFILES in benchbox.mcp (MCP-scoped), inject resolver callable into core via RunRequest.profile_resolver or AdapterFactory closure at execution time. Alternatives rejected: materializing port/secure at admission (persists destination in job store, fails open on retry after withdrawal) and moving registry to benchbox.core (leaks MCP security policy to CLI). Resolver will be surface-supplied, not core-owned.",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "notes": null,
        "started_at": "2026-08-09T02:13:58Z",
        "started_branch": "develop",
        "started_worktree": "/Users/joe/Developer/BenchBox",
        "status": "done",
        "summary": "Decide and record the resolver seam (surface-supplied resolver vs alternatives) before any code moves",
        "wid": "w1"
      },
      {
        "evidence": "w2: verified late-binding property holds: durable job persists only {connection_profile:reviewed} (port/secure absent) via repository.submit, and withdrawn profile fails closed via _run_benchmark_impl (1 passed each). Hoisted-at-admission variant would persist {port:9440,secure:True} and make test_durable_clickhouse_requests_never_persist_a_connection_tuple fail (assert port not in persisted), so ported test would fail under hoisted, confirming late resolution is required. Resolver seam remains surface-supplied (benchbox.mcp) injected at execution, not materialized into RunRequest.",
        "item_id": "one-engine-clickhouse-profile-late-binding",
        "notes": null,
        "started_at": "2026-08-09T02:22:36Z",
        "started_branch": "develop",
        "started_worktree": "/Users/joe/Developer/BenchBox",
        "status": "done",
        "summary": "Port the withdrawal test to the new seam and prove it fails under a hoisted implementation",
        "wid": "w2"
      }
    ]
  }
}
