{
  "context": "These are shared variant queries, so a change affects all platforms. Fix\nby qualifying the correlation column to the outer table (the 10_v8 pattern),\nnot by restructuring the query. Add the affected variants to the DuckDB\nregression EXPLAIN/decorrelation test in tests/integration/test_tpchavoc_duckdb.py.\n",
  "description": "While fixing the DuckDB SF1 hang in tpchavoc 10_v8 (uat-duckdb-reference-triage),\nthe root cause was a correlated subquery whose unqualified columns\n(`l2.l_orderkey = l_orderkey`) silently bound to the INNER alias (l2)\ninstead of the outer table, degenerating the intended correlated\nsemi-join into an uncorrelated full scan. 10_v8 was fixed by qualifying\nthe correlation to the outer table.\n\nTwo more variants carry the identical pattern but do NOT currently break\nany platform (they execute in <0.1s at SF1 on DuckDB because the\ndegenerate subquery folds to a cheap one-time scalar), so they were left\nout of the duckdb-reference-triage scope:\n\n- q11 variant 1 (\"Scalar subqueries\"): subquery\n  `from partsupp ps2, ... where ps2.ps_partkey = ps_partkey` — unqualified\n  `ps_partkey` binds to ps2, so the threshold becomes an uncorrelated\n  avg over all German partsupp rows instead of per-part.\n- q17 variant 8 (\"EXISTS pattern\"): EXISTS subquery\n  `from (...) avg_calc where avg_calc.l_partkey = l_partkey` — unqualified\n  `l_partkey` binds to avg_calc, dropping the per-part correlation.\n\nThese likely produce semantically-incorrect variant results on EVERY\nplatform (tpchavoc validates execution, not result equivalence, so the\ndefect is silent). This item is about correctness of the shared variant\nSQL, not a DuckDB-specific failure.",
  "legacy": {
    "anti_patterns": [
      {
        "dont": "restructure the variant into a different SQL shape",
        "instead": "(not specified)",
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "why": "the variant's identity is its shape; only fix the correlation binding."
      }
    ],
    "item_meta": {
      "category": "Testing and Quality Assurance",
      "completed_at": "2026-06-06T00:00:00Z",
      "created_at": "2026-05-29T00:00:00Z",
      "worktree": "main"
    },
    "preserves": [
      {
        "behavior": "All tpchavoc variants must still parse, bind, and execute on every supported platform.",
        "item_id": "tpchavoc-correlated-subquery-self-binding"
      },
      {
        "behavior": "DuckDB tpchavoc must remain 100% green at 0.01/0.1/1.0 after the changes.",
        "item_id": "tpchavoc-correlated-subquery-self-binding"
      }
    ],
    "prior_art": [
      {
        "concept": "benchbox/core/tpchavoc/variant_sets/q10.yaml:8 — 10_v8 fix qualifies the EXISTS correlation to the outer table (lineitem.l_orderkey); reuse the same qualification approach",
        "decision": "reuse",
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "path": "benchbox/core/tpchavoc/variant_sets/q10.yaml:8"
      },
      {
        "concept": "benchbox/core/tpchavoc/variant_sets/q17.yaml:2 — q17_v2 already qualifies l_partkey -> lineitem.l_partkey for the same class of ambiguity",
        "decision": "reuse",
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "path": "benchbox/core/tpchavoc/variant_sets/q17.yaml:2"
      }
    ],
    "scope_rules": [
      {
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "kind": "only_modify",
        "path_glob": "benchbox/core/tpchavoc/variant_sets/"
      },
      {
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "kind": "only_modify",
        "path_glob": "tests/integration/test_tpchavoc_duckdb.py"
      }
    ],
    "verifications": [
      {
        "command": "uv run -- benchbox run --platform duckdb --benchmark tpchavoc --scale 1.0 --non-interactive --phases load,power",
        "description": "DuckDB tpchavoc still 100% at 1.0",
        "expected": "880/880 executions, 100%",
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "last_result": null,
        "last_run": null,
        "seq": 1,
        "workspace_fingerprint": null
      },
      {
        "command": "uv run -- python -m pytest tests/integration/test_tpchavoc_duckdb.py -q",
        "description": "tpchavoc duckdb integration tests pass",
        "expected": "all pass",
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "last_result": null,
        "last_run": null,
        "seq": 2,
        "workspace_fingerprint": null
      }
    ],
    "work_needs": [
      {
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "needs_wid": "w0",
        "wid": "w1"
      },
      {
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "needs_wid": "w0",
        "wid": "w2"
      },
      {
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "needs_wid": "w0",
        "wid": "w3"
      }
    ],
    "work_units": [
      {
        "evidence": null,
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "notes": "Establish the correct correlated form before editing. The fix is to\nqualify the correlation column to the outer table, matching 10_v8.\nCOMPLETED in PR #756: the intended per-part/per-row correlation was\nconfirmed against canonical TPC-H before qualifying.\n",
        "started_at": null,
        "started_branch": null,
        "started_worktree": null,
        "status": "done",
        "summary": "Confirm intended semantics of q11_v1 and q17_v8 against the base TPC-H query they vary",
        "wid": "w0"
      },
      {
        "evidence": null,
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "notes": "COMPLETED in PR #756. q11.yaml variant 1 now reads\n`ps2.ps_partkey = partsupp.ps_partkey`. A DuckDB EXPLAIN regression assertion\nin tests/integration/test_tpchavoc_duckdb.py confirms it decorrelates into a\ndelimiter join (DELIM present), which the unqualified form did not produce.\n",
        "started_at": null,
        "started_branch": null,
        "started_worktree": null,
        "status": "done",
        "summary": "Qualify the correlation in q11_v1 (ps2.ps_partkey = partsupp.ps_partkey) and verify it decorrelates",
        "wid": "w1"
      },
      {
        "evidence": null,
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "notes": "COMPLETED in PR #756. q17.yaml variant 8 EXISTS subquery now reads\n`avg_calc.l_partkey = lineitem.l_partkey and lineitem.l_quantity < avg_calc.threshold`.\nGuarded by the substring assertion in the new regression test (its second,\nalready-qualified correlation makes the plan check non-discriminating).\n",
        "started_at": null,
        "started_branch": null,
        "started_worktree": null,
        "status": "done",
        "summary": "Qualify the correlation in q17_v8 (avg_calc.l_partkey = lineitem.l_partkey) and verify it decorrelates",
        "wid": "w2"
      },
      {
        "evidence": null,
        "item_id": "tpchavoc-correlated-subquery-self-binding",
        "notes": "COMPLETED in PR #756. The sweep found one additional occurrence,\nq2 variant 8 (NOT EXISTS min-cost), now qualified to\n`ps2.ps_supplycost < partsupp.ps_supplycost`. q2_v8, q11_v1, and q17_v8 were\nadded to the DuckDB regression EXPLAIN list in\ntests/integration/test_tpchavoc_duckdb.py.\n",
        "started_at": null,
        "started_branch": null,
        "started_worktree": null,
        "status": "done",
        "summary": "Sweep all tpchavoc variant_sets for the same alias-self-binding pattern and fix any others",
        "wid": "w3"
      }
    ]
  }
}
