{
  "acceptance": [
    "ClickHouse adapter selects native Delta reads vs Parquet snapshot with availability detection",
    "End-to-end test creates a Delta table and reads it via deltaLake on a Docker host",
    "Pinned server image confirmed to ship the deltaLake integration",
    "System-table probe settles system.functions vs system.table_functions for table-function registration",
    "Legacy ''-doubling staging loader audited for backslash handling"
  ],
  "context": "DEFER home for claude findings 1-3 plus qwen R2/R3/R5. LIVE EVIDENCE 2026-09-22 (mocker/Apple container, image clickhouse-server:25.8, server 25.8.33.6, password benchbox): system.functions lists NO delta functions; system.table_functions lists deltaLake/deltaLakeS3/deltaLakeLocal/deltaLakeAzure (+deltaLakeCluster); system.table_engines lists DeltaLake/DeltaLakeS3/DeltaLakeLocal/DeltaLakeAzure. Probe fixed to system.table_functions in PR 2269 (commit 72a9e1ae7) and the Docker-gated live suite PASSES (2 passed). Manual e2e: deltalake-written table (3 rows) copied into container, deltaLakeLocal count=3 and rows [(1,a),(2,b),(3,c)] exact; ENGINE=DeltaLake('<local path>') fails Code 36 Host-empty-in-S3-URI (engine is URL-only, as documented). REMAINING: adapter native-vs-Parquet selection wiring; S3/Azure argument shapes unexecuted (need storage); legacy ''-doubling audit. NOTE: test_clickhouse_live.py fixture passes no password and fails against this compose (auth) — pre-existing, untouched.",
  "description": "Wire native ClickHouse Delta Lake reads into the adapter and cover them end to end. Use benchbox.platforms.clickhouse.delta_lake builders plus the availability helper to choose native reads vs the Parquet snapshot fallback, and add a data-level live test on a Docker host with a server-visible Delta table."
}
