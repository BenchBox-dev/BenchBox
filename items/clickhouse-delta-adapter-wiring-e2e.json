{
  "acceptance": [
    "ClickHouse adapter selects native Delta reads vs Parquet snapshot with availability detection",
    "End-to-end test creates a Delta table and reads it via deltaLake on a Docker host",
    "Pinned server image confirmed to ship the deltaLake integration",
    "System-table probe settles system.functions vs system.table_functions for table-function registration",
    "Legacy ''-doubling staging loader audited for backslash handling"
  ],
  "context": "Complete. All acceptance criteria met and landed in PR 2290 (branch feat/clickhouse-delta-wiring, OPEN, merge pending maintainer review + CI): adapter delta_native_registration/delta_reader_for with stub-connection unit tests; Docker-gated live suite 5/5 on clickhouse-server 25.8 (server 25.8.33.6) including public-S3 end-to-end reads; system.table_functions confirmed as the registration table (probe fixed); legacy ''-doubling loader audited (low-severity latent inconsistency, intentionally untouched). Manual evidence: deltaLakeLocal count=3 rows exact on a deltalake-written table; ENGINE=DeltaLake URL-only (Code 36 on local paths). NOTE: test_clickhouse_live.py fixture passes no password and fails against the password compose — pre-existing, untouched.",
  "description": "Wire native ClickHouse Delta Lake reads into the adapter and cover them end to end. Use benchbox.platforms.clickhouse.delta_lake builders plus the availability helper to choose native reads vs the Parquet snapshot fallback, and add a data-level live test on a Docker host with a server-visible Delta table."
}
