{
  "acceptance": [
    "ClickHouse adapter selects native Delta reads vs Parquet snapshot with availability detection",
    "End-to-end test creates a Delta table and reads it via deltaLake on a Docker host",
    "Pinned server image confirmed to ship the deltaLake integration",
    "System-table probe settles system.functions vs system.table_functions for table-function registration",
    "Legacy ''-doubling staging loader audited for backslash handling"
  ],
  "context": "DEFER home for claude review findings 1-3 on commit 801e6c352 (PR 2269) plus qwen findings R2/R3/R5/C-b on 1f12d42a4: builders plus a pure availability-detection helper and a registration probe are landed, but no adapter caller, no selection logic, and no data-level read (no Docker in the authoring environment; container cannot see host tmp paths). QWEN-R2: verify whether ClickHouse registers table functions in system.functions or system.table_functions by running both LIKE 'deltaLake%' probes on the pinned server and fix delta_function_probe_sql to the correct table. QWEN-C6: audit the legacy ''-doubling staging loader (benchbox/platforms/clickhouse_cloud.py) for backslash passthrough. Wiring and e2e belong here, not in the builders commit.",
  "description": "Wire native ClickHouse Delta Lake reads into the adapter and cover them end to end. Use benchbox.platforms.clickhouse.delta_lake builders plus the availability helper to choose native reads vs the Parquet snapshot fallback, and add a data-level live test on a Docker host with a server-visible Delta table."
}
