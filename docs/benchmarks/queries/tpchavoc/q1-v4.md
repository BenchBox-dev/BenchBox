<!-- Copyright 2026 Joe Harris / BenchBox Project. Licensed under the MIT License. -->
<!-- AUTOGENERATED by scripts/generate_query_docs.py from benchbox.core.query_catalog. Do not edit by hand. Run `uv run scripts/generate_query_docs.py` (or `make query-docs`) to refresh. -->

# TPC-Havoc Q1_v4

```{tags} reference, query-template, tpchavoc
```

> [TPC-Havoc](../../tpc-havoc.md) benchmark · [all TPC-Havoc queries](index.md)

## Representative SQL

Rendered for **DataFusion** with default parameters.

```sql
SELECT final.l_returnflag, final.l_linestatus, final.sum_qty, final.sum_base_price, final.sum_disc_price, final.sum_charge, final.sum_qty / final.count_order AS avg_qty, final.sum_base_price / final.count_order AS avg_price, final.sum_disc / final.count_order AS avg_disc, final.count_order FROM (SELECT l_returnflag, l_linestatus, SUM(l_quantity) AS sum_qty, SUM(l_extendedprice) AS sum_base_price, SUM(l_extendedprice * (1 - l_discount)) AS sum_disc_price, SUM(l_extendedprice * (1 - l_discount) * (1 + l_tax)) AS sum_charge, SUM(l_discount) AS sum_disc, COUNT(*) AS count_order FROM lineitem WHERE l_shipdate <= CAST('1998-12-01' AS DATE) - INTERVAL '90 DAY' GROUP BY l_returnflag, l_linestatus UNION ALL SELECT l_returnflag, l_linestatus, 0 AS sum_qty, 0 AS sum_base_price, 0 AS sum_disc_price, 0 AS sum_charge, 0 AS sum_disc, 0 AS count_order FROM lineitem WHERE l_shipdate <= CAST('1998-12-01' AS DATE) - INTERVAL '90 DAY' GROUP BY l_returnflag, l_linestatus HAVING 1 = 0 /* Empty result to test UNION ALL handling */) AS final WHERE final.count_order > 0 ORDER BY final.l_returnflag, final.l_linestatus
```

## Representative DataFrame

_No DataFrame implementation is registered for this query._

## Get the exact query for your run

The blocks above are representative. To get the precise statement a specific platform runs at a specific scale, substitute your own values:

```python
from benchbox import TPCHavoc

bench = TPCHavoc(scale_factor=0.01)            # your scale factor
queries = bench.get_queries(dialect="datafusion")   # your platform's dialect; drop dialect= for native SQL
print(queries['1_v4'])
```

## Source

Query text: [`benchbox/core/tpchavoc/queries.py`](../../../../benchbox/core/tpchavoc/queries.py)
