<!-- Copyright 2026 Joe Harris / BenchBox Project. Licensed under the MIT License. -->
<!-- AUTOGENERATED by scripts/generate_query_docs.py from benchbox.core.query_catalog. Do not edit by hand. Run `uv run scripts/generate_query_docs.py` (or `make query-docs`) to refresh. -->

# TPC-H Skew Q15 — Top Supplier

```{tags} reference, query-template, tpch_skew
```

> [TPC-H Skew](../../tpch-skew.md) benchmark · [all TPC-H Skew queries](index.md)

Determine top supplier based on revenue

## Representative SQL

Rendered for **DataFusion** with default parameters.

```sql
WITH revenue(supplier_no, total_revenue) AS (SELECT l_suppkey, SUM(l_extendedprice * (1 - l_discount)) FROM lineitem WHERE l_shipdate >= CAST('1996-01-01' AS DATE) AND l_shipdate < CAST('1996-01-01' AS DATE) + INTERVAL '3 MONTH' GROUP BY l_suppkey) SELECT s_suppkey, s_name, s_address, s_phone, total_revenue FROM supplier, revenue WHERE s_suppkey = supplier_no AND total_revenue = (SELECT MAX(total_revenue) FROM revenue) ORDER BY s_suppkey
```

## Representative DataFrame

Expression family (Polars, PySpark, DataFusion), default parameters.

```python
def q15_expression_impl(ctx: DataFrameContext) -> Any:
    """TPC-H Q15: Top Supplier (Expression Family).

    Determines the top supplier based on revenue from lineitems.
    """
    supplier = ctx.get_table("supplier")
    lineitem = ctx.get_table("lineitem")
    col = ctx.col
    lit = ctx.lit

    params = get_tpch_parameters(15)
    start_date = params["start_date"]
    end_date = params["end_date"]

    # Calculate revenue per supplier (CTE-like pattern)
    revenue = (
        lineitem.filter((col("l_shipdate") >= lit(start_date)) & (col("l_shipdate") < lit(end_date)))
        .group_by(col("l_suppkey").alias("supplier_no"))
        .agg((col("l_extendedprice") * (lit(1) - col("l_discount"))).sum().alias("total_revenue"))
    )

    # Find maximum revenue using optimized scalar extraction
    max_revenue = ctx.scalar(revenue.select(col("total_revenue").max().alias("max_rev")))

    # Join with suppliers having maximum revenue
    return (
        supplier.join(revenue, left_on="s_suppkey", right_on="supplier_no")
        .filter(col("total_revenue") == lit(max_revenue))
        .select("s_suppkey", "s_name", "s_address", "s_phone", "total_revenue")
        .sort("s_suppkey")
    )
```

## Get the exact query for your run

The blocks above are representative. To get the precise statement a specific platform runs at a specific scale, substitute your own values:

```python
from benchbox import TPCHSkew

bench = TPCHSkew(scale_factor=0.01)            # your scale factor
queries = bench.get_queries(dialect="datafusion")   # your platform's dialect; drop dialect= for native SQL
print(queries['15'])
```

Parameter substitution for this benchmark is seed-driven; pass a `seed` for a specific stream's parameters:

```python
print(bench.get_query(15, scale_factor=0.01, seed=42, dialect="datafusion"))
```

For the DataFrame form:

```python
from benchbox.core.query_catalog import get_dataframe_render

print(get_dataframe_render('tpch_skew', '15', family="expression").source)  # or family="pandas"
```

## Source

Query text: [`benchbox/_binaries/tpc-h/templates/queries/15.sql`](../../../../benchbox/_binaries/tpc-h/templates/queries/15.sql)
