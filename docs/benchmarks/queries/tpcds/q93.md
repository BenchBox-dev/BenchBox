<!-- Copyright 2026 Joe Harris / BenchBox Project. Licensed under the MIT License. -->
<!-- AUTOGENERATED by scripts/generate_query_docs.py from benchbox.core.query_catalog. Do not edit by hand. Run `uv run scripts/generate_query_docs.py` (or `make query-docs`) to refresh. -->

# TPC-DS Q93 — Store Sales Returns Actual Sales

```{tags} reference, query-template, tpcds
```

> [TPC-DS](../../tpc-ds.md) benchmark · [all TPC-DS queries](index.md)

Store sales with returns computing actual sales by customer filtered by reason

## Representative SQL

Rendered for **DataFusion** with default parameters.

```sql
SELECT
  ss_customer_sk,
  SUM(act_sales) AS sumsales
FROM (
  SELECT
    ss_item_sk,
    ss_ticket_number,
    ss_customer_sk,
    CASE
      WHEN NOT sr_return_quantity IS NULL
      THEN (
        ss_quantity - sr_return_quantity
      ) * ss_sales_price
      ELSE (
        ss_quantity * ss_sales_price
      )
    END AS act_sales
  FROM store_sales
  LEFT OUTER JOIN store_returns
    ON (
      sr_item_sk = ss_item_sk AND sr_ticket_number = ss_ticket_number
    ), reason
  WHERE
    sr_reason_sk = r_reason_sk AND r_reason_desc = 'Did not like the warranty'
) AS t
GROUP BY
  ss_customer_sk
ORDER BY
  sumsales,
  ss_customer_sk
LIMIT 100
```

## Representative DataFrame

Expression family (Polars, PySpark, DataFusion), default parameters.

```python
def q93_expression_impl(ctx: DataFrameContext) -> Any:
    """Q93: Store sales with returns actual sales calculation (Polars).

    Left joins store_sales with store_returns to compute actual sales
    (sales - returns) filtered by return reason.

    Tables: store_sales, store_returns, reason
    """
    params = get_parameters(93)
    reason = params.get("reason", "reason 28")

    store_sales, store_returns = _tables(ctx, "store_sales", "store_returns")
    reason_table = ctx.get_table("reason")
    col = ctx.col
    lit = ctx.lit

    # Filter reason
    reason_filtered = reason_table.filter(col("r_reason_desc") == lit(reason))

    # Left join store_sales with store_returns
    ss_with_sr = store_sales.join(
        store_returns,
        left_on=["ss_item_sk", "ss_ticket_number"],
        right_on=["sr_item_sk", "sr_ticket_number"],
        how="left",
    )

    # Join with reason
    ss_with_reason = ss_with_sr.join(reason_filtered, left_on="sr_reason_sk", right_on="r_reason_sk")

    # Compute actual sales
    result = ss_with_reason.with_columns(
        ctx.when(col("sr_return_quantity").is_not_null())
        .then((col("ss_quantity") - col("sr_return_quantity")) * col("ss_sales_price"))
        .otherwise(col("ss_quantity") * col("ss_sales_price"))
        .alias("act_sales")
    )

    # Aggregate by customer
    return (
        result.group_by("ss_customer_sk")
        .agg(ctx.sum("act_sales").alias("sumsales"))
        .sort(["sumsales", "ss_customer_sk"])
        .head(100)
    )
```

## Get the exact query for your run

The blocks above are representative. To get the precise statement a specific platform runs at a specific scale, substitute your own values:

```python
from benchbox import TPCDS

bench = TPCDS(scale_factor=1.0)            # your scale factor
queries = bench.get_queries(dialect="datafusion")   # your platform's dialect; drop dialect= for native SQL
print(queries['93'])
```

Parameter substitution for this benchmark is seed-driven; pass a `seed` for a specific stream's parameters:

```python
print(bench.get_query(93, scale_factor=1.0, seed=42, dialect="datafusion"))
```

For the DataFrame form:

```python
from benchbox.core.query_catalog import get_dataframe_render

print(get_dataframe_render('tpcds', '93', family="expression").source)  # or family="pandas"
```

## Source

Query text: [`_sources/tpc-ds/query_templates/query93.tpl`](../../../../_sources/tpc-ds/query_templates/query93.tpl)
