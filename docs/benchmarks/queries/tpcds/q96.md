<!-- Copyright 2026 Joe Harris / BenchBox Project. Licensed under the MIT License. -->
<!-- AUTOGENERATED by scripts/generate_query_docs.py from benchbox.core.query_catalog. Do not edit by hand. Run `uv run scripts/generate_query_docs.py` (or `make query-docs`) to refresh. -->

# TPC-DS Q96 — Store Sales Time Count

```{tags} reference, query-template, tpcds
```

> [TPC-DS](../../tpc-ds.md) benchmark · [all TPC-DS queries](index.md)

Counts store sales for specific time periods

## Representative SQL

Rendered for **DataFusion** with default parameters.

```sql
SELECT COUNT(*) FROM store_sales, household_demographics, time_dim, store WHERE ss_sold_time_sk = time_dim.t_time_sk AND ss_hdemo_sk = household_demographics.hd_demo_sk AND ss_store_sk = s_store_sk AND time_dim.t_hour = 8 AND time_dim.t_minute >= 30 AND household_demographics.hd_dep_count = 5 AND store.s_store_name = 'ese' ORDER BY COUNT(*) LIMIT 100
```

## Representative DataFrame

Expression family (Polars, PySpark, DataFusion), default parameters.

```python
def q96_expression_impl(ctx: DataFrameContext) -> Any:
    """TPC-DS Q96: Store Sales Time (Expression Family).

    Counts store sales for specific time periods.

    Tables: store_sales, household_demographics, time_dim, store
    Pattern: 4-way join -> filter -> count
    """
    time_dim, store_sales, store, household_demographics = _tables(
        ctx, "time_dim", "store_sales", "store", "household_demographics"
    )
    col = ctx.col
    lit = ctx.lit

    params = get_parameters(96)
    hours = params.get("hours", [(8, 9)])
    t_hour = hours[0][0]

    return (
        store_sales.join(time_dim, left_on="ss_sold_time_sk", right_on="t_time_sk")
        .join(store, left_on="ss_store_sk", right_on="s_store_sk")
        .join(household_demographics, left_on="ss_hdemo_sk", right_on="hd_demo_sk")
        .filter(
            (col("t_hour") == lit(t_hour))
            & (col("t_minute") >= lit(30))
            & (col("hd_dep_count") == lit(0))
            & (col("s_number_employees") >= lit(200))
            & (col("s_number_employees") <= lit(295))
        )
        .select(col("ss_sold_time_sk").count().alias("count"))
    )
```

## Get the exact query for your run

The blocks above are representative. To get the precise statement a specific platform runs at a specific scale, substitute your own values:

```python
from benchbox import TPCDS

bench = TPCDS(scale_factor=1.0)            # your scale factor
queries = bench.get_queries(dialect="datafusion")   # your platform's dialect; drop dialect= for native SQL
print(queries['96'])
```

Parameter substitution for this benchmark is seed-driven; pass a `seed` for a specific stream's parameters:

```python
print(bench.get_query(96, scale_factor=1.0, seed=42, dialect="datafusion"))
```

For the DataFrame form:

```python
from benchbox.core.query_catalog import get_dataframe_render

print(get_dataframe_render('tpcds', '96', family="expression").source)  # or family="pandas"
```

## Source

Query text: [`_sources/tpc-ds/query_templates/query96.tpl`](../../../../_sources/tpc-ds/query_templates/query96.tpl)
