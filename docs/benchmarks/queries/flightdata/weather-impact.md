<!-- Copyright 2026 Joe Harris / BenchBox Project. Licensed under the MIT License. -->
<!-- AUTOGENERATED by scripts/generate_query_docs.py from benchbox.core.query_catalog. Do not edit by hand. Run `uv run scripts/generate_query_docs.py` (or `make query-docs`) to refresh. -->

# Flight Data — Weather Delay Impact by Month

```{tags} reference, query-template, flightdata
```

> [Flight Data](../../flightdata.md) benchmark · [all Flight Data queries](index.md)

Average weather delay by month showing seasonal patterns

## Representative SQL

Rendered for **DataFusion** with default parameters.

```sql
SELECT
  f.month,
  COUNT(*) AS total_flights,
  SUM(CASE WHEN f.weather_delay > 0 THEN 1 ELSE 0 END) AS weather_delayed,
  ROUND(
    CAST(100.0 * SUM(CASE WHEN f.weather_delay > 0 THEN 1 ELSE 0 END) / COUNT(*) AS DECIMAL),
    2
  ) AS weather_delay_rate_pct,
  ROUND(CAST(AVG(CASE WHEN f.weather_delay > 0 THEN f.weather_delay END) AS DECIMAL), 2) AS avg_weather_delay_min,
  ROUND(SUM(COALESCE(f.weather_delay, 0)), 0) AS total_weather_minutes
FROM flights AS f
WHERE
  f.cancelled = 0 AND f.flight_date >= '2021-08-01' AND f.flight_date < '2025-01-01'
GROUP BY
  f.month
ORDER BY
  f.month
```

## Representative DataFrame

Expression family (Polars, PySpark, DataFusion), default parameters.

```python
def weather_impact_expression_impl(ctx: DataFrameContext) -> Any:
    flights, col, lit = _date_window(ctx, lambda col, lit: col("cancelled") == lit(0))

    return (
        flights.group_by("month")
        .agg(
            col("flight_id").count().alias("total_flights"),
            (col("weather_delay") > lit(0)).cast(int).sum().alias("weather_delayed"),
            col("weather_delay").filter(col("weather_delay") > lit(0)).mean().round(2).alias("avg_weather_delay_min"),
            col("weather_delay").fill_null(lit(0)).sum().round(0).alias("total_weather_minutes"),
        )
        .with_columns(
            (lit(100.0) * col("weather_delayed") / col("total_flights")).round(2).alias("weather_delay_rate_pct")
        )
        .sort("month")
    )
```

## Get the exact query for your run

The blocks above are representative. To get the precise statement a specific platform runs at a specific scale, substitute your own values:

```python
from benchbox import FlightData

bench = FlightData(scale_factor=1.0)            # your scale factor
queries = bench.get_queries(dialect="datafusion")   # your platform's dialect; drop dialect= for native SQL
print(queries['weather-impact'])
```

For the DataFrame form:

```python
from benchbox.core.query_catalog import get_dataframe_render

print(get_dataframe_render('flightdata', 'weather-impact', family="expression").source)  # or family="pandas"
```

## Source

Query text: [`benchbox/core/flightdata/queries.py`](../../../../benchbox/core/flightdata/queries.py)
