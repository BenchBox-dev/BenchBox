<!-- Copyright 2026 Joe Harris / BenchBox Project. Licensed under the MIT License. -->
<!-- AUTOGENERATED by scripts/generate_query_docs.py from benchbox.core.query_catalog. Do not edit by hand. Run `uv run scripts/generate_query_docs.py` (or `make query-docs`) to refresh. -->

# TPC-H Q17 — Small-Quantity-Order Revenue

```{tags} reference, query-template, tpch
```

> [TPC-H](../../tpc-h.md) benchmark · [all TPC-H queries](index.md)

Revenue from eliminating small quantity orders

## Representative SQL

Rendered for **DataFusion** with default parameters.

```sql
SELECT SUM(l_extendedprice) / 7.0 AS avg_yearly FROM lineitem, part WHERE p_partkey = l_partkey AND p_brand = 'Brand#23' AND p_container = 'MED BOX' AND l_quantity < (SELECT 0.2 * AVG(l_quantity) FROM lineitem WHERE l_partkey = p_partkey)
```

## Representative DataFrame

Expression family (Polars, PySpark, DataFusion), default parameters.

```python
def q17_expression_impl(ctx: DataFrameContext) -> Any:
    """TPC-H Q17: Small-Quantity-Order Revenue (Expression Family).

    Determines potential revenue increase from eliminating small quantity orders.
    """
    lineitem = ctx.get_table("lineitem")
    part = ctx.get_table("part")
    col = ctx.col
    lit = ctx.lit

    params = get_tpch_parameters(17)
    brand = params["brand"]
    container = params["container"]

    # Calculate average quantity per part
    avg_qty_per_part = lineitem.group_by("l_partkey").agg((col("l_quantity").mean() * lit(0.2)).alias("avg_qty"))

    # Main query
    return (
        part.filter((col("p_brand") == lit(brand)) & (col("p_container") == lit(container)))
        .join(lineitem, left_on="p_partkey", right_on="l_partkey")
        .join(avg_qty_per_part, left_on="p_partkey", right_on="l_partkey")
        .filter(col("l_quantity") < col("avg_qty"))
        .select((col("l_extendedprice").sum() / lit(7.0)).alias("avg_yearly"))
    )
```

## Get the exact query for your run

The blocks above are representative. To get the precise statement a specific platform runs at a specific scale, substitute your own values:

```python
from benchbox import TPCH

bench = TPCH(scale_factor=0.01)            # your scale factor
queries = bench.get_queries(dialect="datafusion")   # your platform's dialect; drop dialect= for native SQL
print(queries['17'])
```

Parameter substitution for this benchmark is seed-driven; pass a `seed` for a specific stream's parameters:

```python
print(bench.get_query(17, scale_factor=0.01, seed=42, dialect="datafusion"))
```

For the DataFrame form:

```python
from benchbox.core.query_catalog import get_dataframe_render

print(get_dataframe_render('tpch', '17', family="expression").source)  # or family="pandas"
```

## Source

Query text: [`benchbox/_binaries/tpc-h/templates/queries/17.sql`](../../../../benchbox/_binaries/tpc-h/templates/queries/17.sql)
