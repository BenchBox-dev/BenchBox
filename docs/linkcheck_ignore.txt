# External URLs that are known to be flaky, rate-limited, or login-gated.
# Keep this list focused and prune entries when upstream stability improves.

# Local examples in docs (not reachable in CI)
http://localhost(:\d+)?/.*

# TPC pages are frequently unavailable to automated checks
http://www\.tpc\.org/.*
https://www\.tpc\.org/.*

# Newsletter/archive host intermittently blocks bots
https://buttondown\.com/.*

# Login-protected or interactive portals
https://app\.motherduck\.com/.*
https://cloud2\.influxdata\.com/.*
https://api\.onehouse\.ai/.*

# Package index endpoint can reject crawler requests
https://pypi\.nvidia\.com/.*

# ISO standards pages may block automated requests
https://www\.iso\.org/standard/.*

# SIGMOD intermittently refuses automated link checks
https://sigmod\.org/.*

# SelectDB portal intermittently drops connections
https://selectdb\.com

# Okabe-Ito color palette page intermittently returns 500
https://jfly\.uni-koeln\.de/.*

# NO_COLOR spec host: unresolvable from CI runners (DNS NameResolutionError)
https://no-color\.org/.*

# AWS documentation rejects automated CI requests with HTTP 403 (Redshift and
# S3 pages); the links remain visible in the rendered documentation.
https://docs\.aws\.amazon\.com/(redshift|s3)/.*

# H2O documentation rejects automated CI requests with HTTP 403.
https://docs\.h2o\.ai/.*

# HammerDB site intermittently returns 502 / connect timeouts
https://www\.hammerdb\.com/.*

# GitHub blob/issues links in blog posts intermittently return 404/429 to
# unauthenticated CI requests once rate-limited; verified reachable (200) from
# an authenticated/non-rate-limited client.
https://github\.com/joeharris76/BenchBox/(blob|issues).*
