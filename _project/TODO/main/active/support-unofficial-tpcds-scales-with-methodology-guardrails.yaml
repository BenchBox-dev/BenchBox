id: support-unofficial-tpcds-scales-with-methodology-guardrails
title: "Support unofficial TPC-DS scales with explicit methodology guardrails"
worktree: main
priority: High
status: In Progress
category: Core Functionality

description: |
  BenchBox currently rejects TPC-DS scale factors below 1.0 in multiple places
  because the bundled native `dsdgen` path is believed to crash at fractional
  scales, and a plain `tpcds_sf001` sample directory exists only as a
  placeholder. At the same time, unofficial-vs-official TPC-DS comparability is
  not modeled as a first-class result property: result building, schema export,
  CLI summaries, report import, publication metadata, and historical rankings
  all assume that benchmark + scale factor is enough to determine comparability.

  This item defines the work needed to support non-official TPC-DS scales for
  developer workflows without letting those runs masquerade as official or
  comparable results. The first gate is empirical: prove what the bundled
  `dsdgen` binaries actually do at subscale and document the resulting table
  contract before any product behavior changes merge. If native generation is
  only partially viable, continue with per-platform capability gating
  (`native_subscale` vs `user_supplied_only`) rather than collapsing to an
  all-or-nothing outcome. If all bundled binaries fail, stop implementation and
  spawn a narrower follow-up for patched binaries or user-supplied/
  pre-generated datasets rather than forcing the current validation and
  reporting pipeline into an incoherent state.

  Gate Zero on 2026-04-13 failed on the locally executable bundled macOS
  binaries (`darwin-arm64`, `darwin-x86_64`): required small-dimension tables
  segfault at `sf=0.01`, `0.1`, and `0.5`, and the repo's bundled
  `examples/data/tpcds_sf001` asset is still only a placeholder. See
  `_sources/tpcds-subscale-probe.md`. A follow-up minimal repro removed
  BenchBox wrapper logic from the boundary: direct invocation of the packaged
  host-native binary crashed in the same way, and a fresh local build from
  `_sources/tpc-ds/tools` reproduced the same `call_center sf=0.01` failure on
  macOS arm64. `lldb` pinned the fault to upstream generator code at
  `_sources/tpc-ds/tools/dist.c:512` (`dist_weight`, null dereference of
  `weight_sets[wset - 1]` with `wset=0`), reached via
  `_sources/tpc-ds/tools/scaling.c:124` (`LogScale`),
  `_sources/tpc-ds/tools/scaling.c:319` (`get_rowcount`),
  `_sources/tpc-ds/tools/parallel.c:64` (`split_work`), and
  `_sources/tpc-ds/tools/driver.c:549` (`main`). This item is blocked until the
  bundled/upstream generator problem is solved in a separate track.

deps:
  needs:
    - patch-and-redistribute-tpcds-dsdgen-subscale-support

work:
- id: w1
  summary: "Run Gate Zero: probe every bundled TPC-DS dsdgen binary at 0.01, 0.1, and 0.5 and document actual behavior"
  status: done
  notes: |
    Required research gate. For each bundled binary, record exit status, stderr,
    produced tables, row counts, and checksums, with special attention to small
    dimension tables such as call_center, store, warehouse, and web_site.
    Output: `_sources/tpcds-subscale-probe.md`.

    Result on 2026-04-13: both locally executable bundled macOS binaries
    (`darwin-arm64`, `darwin-x86_64`) exited 139 with no output files for
    `call_center`, `store`, `warehouse`, and `web_site` at `sf=0.01`, `0.1`,
    and `0.5`; `sf=1.0` controls succeeded with expected row counts and stable
    checksums. Linux and Windows bundles are present but were not executable on
    this Darwin host. The bundled `examples/data/tpcds_sf001` directory
    contains only a placeholder README, so there is no real sample-data escape
    hatch today. A direct-binary repro on the same host proved the crash
    occurs before any BenchBox wrapper logic runs, and a fresh source build
    from `_sources/tpc-ds/tools` reproduced the same failure. `lldb` on the
    source-built binary stopped in `dist_weight` at
    `_sources/tpc-ds/tools/dist.c:512`, called from
    `_sources/tpc-ds/tools/scaling.c:124`,
    `_sources/tpc-ds/tools/scaling.c:319`,
    `_sources/tpc-ds/tools/parallel.c:64`, and
    `_sources/tpc-ds/tools/driver.c:549`.

    If some platforms pass and some fail, continue implementation with an
    explicit per-platform capability split (`native_subscale` vs
    `user_supplied_only`) and keep subscale generation gated on the current
    platform capability. Do not flatten a mixed outcome into a global yes/no.

    If all platforms fail, stop implementation and create a follow-up TODO for
    patched binaries or user-supplied/pre-generated data. Do not merge any
    behavior changes based on assumption alone.

- id: w10
  summary: "Use DuckDB's TPC-DS tooling as a diagnostic cross-check for schema and downstream assumptions"
  needs: [w1]
  status: done
  notes: |
    Completed 2026-04-14. Output: `_sources/tpcds-duckdb-diagnostic.md`.

    DuckDB v1.2.2 with the tpcds extension generates all 24 BenchBox-canonical
    tables at sf=0.01, sf=0.1, and sf=1.0 without crashes. Column counts match
    dsdgen sf=1.0 for all four Gate Zero tables (call_center=31, store=29,
    warehouse=14, web_site=26). 22/24 sf=1.0 row counts match BenchBox
    expectations exactly; store_returns and web_returns differ by <0.2% (well
    within the 5% tolerance). The `dbgen_version` table is absent from DuckDB
    output — manifest expected_table_count (25) would need adjustment for any
    non-dsdgen source.

    Critical finding for w2 (data contract): BenchBox's FIXED_TABLES set
    assumes call_center, store, warehouse, web_site, and reason always hold
    their sf=1.0 row counts. DuckDB shows these tables actually scale down at
    sf<1.0 (e.g., call_center=1 at sf=0.01). A subscale data contract must not
    assume fixed-table row counts hold below sf=1.0.

    Load compatibility confirmed: BenchBox's has_trailing_delimiter detection
    handles both dsdgen pipe-terminated and DuckDB pipe-separated formats.
    The scale_factor >= 1.0 guard lives only in the generator and runner
    layers (benchmark_registry.py:546 and runner.py:162); the data loading path
    does not check scale_factor at all. Downstream load and execution code does
    not inherently require official scale points.

    DuckDB remains diagnostic only. It is not a substitute for dsdgen output
    in BenchBox's methodology context.

- id: w2
  summary: "Define the subscale data contract from probe results: missing-vs-empty tables, manifest expectations, and validation
    rules"
  needs: [w1]
  status: done
  notes: |
    Completed on 2026-04-14. Full contract in _sources/tpcds-subscale-contract.md.

    Key decisions:
    - All 25 tables mandatory (no missing-table exceptions); min-1-row floor ensures this.
    - "Fixed-size" table exemption does NOT apply at sf < 1.0 — fixed tables scale
      proportionally with max(1, floor(sf1_baseline * sf)) like all other tables.
    - Same 5% validation tolerance applies at subscale; min-1 floor ensures expected_rows > 0.
    - New manifest field: compliance_class = "unofficial_subscale" for sf < 1.0 runs.
    - Official TPC metrics (power_at_size, throughput_at_size, qph_at_size) must not be
      computed for unofficial_subscale compliance class.
    - Non-official sf >= 1.0 runs do NOT require new opt-in; only sf < 1.0 is gated.
    - Windows bundles remain unpatched; must surface a clear error (not a crash).

- id: w3
  summary: "Add a single TPC-DS compliance classifier and remove contradictory scale handling"
  needs: [w1]
  status: done
  notes: |
    Completed on 2026-04-14. New module: benchbox/core/tpcds/compliance.py.

    Three changes:
    1. Created TpcdsComplianceClass enum (OFFICIAL, UNOFFICIAL_NONSTANDARD, UNOFFICIAL_SUBSCALE)
       with classify_tpcds_run() and validate_tpcds_scale() functions. Single source of truth.
    2. Removed the silent scale_factor < 1.0 → 1.0 rounding in benchmark/runner.py.
       Replaced with validate_tpcds_scale() call (clear error until w4 wires opt-in).
    3. Both generator/manager.py and benchmark_registry.py now delegate to validate_tpcds_scale.
       The allow_unofficial_scale=False parameter exists in both; w4 will wire the CLI flag through.
    4. benchmark_registry.py min_scale removed for tpcds; compliance.py owns that boundary.

    Behavior decision: non-official sf>=1.0 runs unchanged. Only sf<1.0 is newly gated
    (with a clear error pointing to --allow-unofficial-scale). No retroactive opt-in required
    for existing non-official SF>=1.0 workflows.

- id: w4
  summary: "Wire explicit CLI opt-in and warning banners through `run` and `datagen`"
  needs: [w3]
  status: pending
  notes: |
    Add `--allow-unofficial-scale` to `benchbox run` and forward it through the
    `benchbox datagen` wrapper. Emit a non-interactive warning banner that names
    the exact scale factor, the compliance bucket, and the downstream
    publish/submit restrictions.

- id: w5
  summary: "Make compliance durable across result objects, schema export/import, loader, normalizer, database, and publication
    metadata"
  needs: [w2, w3]
  status: pending
  notes: |
    Add first-class methodology/comparability fields to the result pipeline and
    persist them end-to-end. Do not rely on a nested
    `execution_metadata[\"compliance\"]` object alone: schema export currently
    cherry-picks known fields, and the historical database does not persist
    arbitrary execution metadata.

- id: w6
  summary: "Suppress official TPC metrics and isolate unofficial TPC-DS runs in summaries, rankings, visualizations, and comparisons"
  needs: [w5]
  status: pending
  notes: |
    Gate `power_at_size`, `throughput_at_size`, and `qph_at_size` in the generic
    result path, not just in TPC-DS report templates. Update CLI summaries,
    visualization surfaces, and ranking/report logic so unofficial TPC-DS runs
    render clearly as non-comparable and do not contaminate official cohorts.

    This explicitly includes `benchbox/core/visualization/` and
    `benchbox/mcp/tools/visualization.py` plus associated visualization/MCP
    tests, not just CLI tables or legacy TPC-DS reporting templates.

- id: w7
  summary: "Align generation, manifest writing, loading, and validation with the measured unofficial subscale contract"
  needs: [w2, w3]
  status: pending
  notes: |
    Reconcile the current mismatch where generation/loading may skip empty or
    missing subscale artifacts but validation still requires a full 25-table
    TPC-DS dataset. Keep methodology-specific validation strict; do not replace
    it with broad best-effort behavior.

- id: w8
  summary: "Add filename/publication markers plus exact submit, publish, and report-import guardrails"
  needs: [w5, w6]
  status: pending
  notes: |
    Preserve existing `sf001` naming. Mark unofficial artifacts with an adjacent
    unofficial token and carry the same distinction into publication metadata.
    `submit` must refuse any non-official result with no override. `publish`
    must refuse non-official results unless the user opts in with
    `--label unofficial-research`, and that label plus the compliance fields
    must persist in the published manifest.

    `report import` and comparative reporting surfaces must exclude unofficial
    TPC-DS artifacts by default and include them only via explicit opt-in with
    clear non-comparable markers.

- id: w9
  summary: "Update contributor docs and run the full verification matrix after implementation"
  needs: [w4, w5, w6, w7, w8]
  status: pending
  notes: |
    Update CLAUDE.md, GEMINI.md, AGENTS.md, `_sources/README.md`, and add an ADR
    only once the implementation path is proven. Verification must cover the
    probe artifact, targeted TPC-DS tests, result export/import round-trip,
    rankings isolation, and the standard fast suite.

deferred:
- summary: "Patch and redistribute upstream dsdgen binaries if Gate Zero shows native subscale generation is not viable"
  reason: "Separate licensing, build, and distribution project; do not hide it inside feature work."
- summary: "Introduce a sample-data or user-supplied-data fallback workflow for unofficial TPC-DS runs"
  reason: "Only pursue if Gate Zero blocks native generation; the contract depends on the probe outcome."
- summary: "Refactor global scale parsing to a Decimal-backed abstraction"
  reason: "No evidence yet that existing float handling is the blocker; avoid adding complexity before a concrete need exists."

metadata:
  tags:
  - tpcds
  - unofficial
  - scale-factor
  - results
  - validation
  - publishing
  estimated_effort: "Large"
  created_date: "2026-04-13"

impact:
  business_value: |
    Unlocks a realistic local-development path for TPC-DS without weakening
    BenchBox's claims around official TPC comparability or polluting published
    result sets with ambiguous artifacts.
  user_impact: |
    Contributors can iterate on TPC-DS at smaller scales once the methodology is
    proven, while reviewers and result consumers get unmistakable signals about
    what is and is not comparable.
  technical_debt: |
    Fixes the current split-brain state where TPC-DS scale handling, validation,
    and reporting each encode partial assumptions about official compliance and
    dataset completeness.

must_preserve:
- "Official TPC-DS workflows remain unchanged for comparable runs, including `benchbox run --official` behavior and official-scale
  artifact semantics."
- "Shared `format_scale_factor()` tokens and existing path conventions remain stable for official runs and for other benchmarks
  that already use fractional scales."
- "Historical reporting, publication, and rankings must not silently mix unofficial TPC-DS artifacts into official comparable
  cohorts."

approach: |
  Start with the empirical Gate Zero probe and write down the table-level data
  contract before touching validators or user-facing behavior. Then introduce a
  single TPC-DS compliance classifier, add a deliberate CLI opt-in, and make
  methodology/comparability durable across the full result pipeline: result
  object, schema export, import/load, database storage, publication metadata,
  ranking, and display.

  Reuse the existing `format_scale_factor()` tokenization and append an explicit
  unofficial marker rather than inventing a new path scheme. Suppress official
  TPC metrics in the generic result builder and summary/rendering layers, not
  only in legacy TPC-DS reporting code.

anti_patterns:
- "DO NOT enable unofficial TPC-DS scales by only removing the current min-scale checks -- because generation, validation,
  storage, and reporting remain inconsistent -- prove binary behavior and data contract first."
- "DO NOT store compliance only under `execution_metadata` and assume it will round-trip -- because schema export/import and
  database rankings ignore arbitrary metadata today -- add explicit result/schema/storage support."
- "DO NOT replace `sf001` token formatting with a new scheme -- because the existing formatter already drives filenames, paths,
  database names, and sample lookup -- preserve the token and add an adjacent unofficial marker."
- "DO NOT relax TPC-DS missing-table validation generically -- because current generation/loading behavior already diverges
  from validators -- derive methodology-specific rules from Gate Zero evidence and enforce those."
- "DO NOT switch the product path to DuckDB-generated TPC-DS data just because DuckDB exposes nonstandard scale handling --
  because that changes generator provenance and methodology semantics -- use DuckDB only as a diagnostic cross-check unless
  product requirements explicitly change."

verification:
- description: "New TODO validates against the project TODO schema"
  command: "uv run _project/scripts/validate_todo.py _project/TODO/main/active/support-unofficial-tpcds-scales-with-methodology-guardrails.yaml"
  expected_output: "validation passes with no schema errors"
- description: "TODO dependency graph remains valid after adding the new item"
  command: "uv run _project/scripts/todo_cli.py check-graph"
  expected_output: "graph validation passes with no cycles or dangling references"
- description: "Indexes regenerate cleanly after the new TODO is added"
  command: "uv run _project/scripts/generate_indexes.py"
  expected_output: "TODO indexes regenerate successfully"
- description: "Implementation verification covers both general fast tests and targeted TPC-DS/result-pipeline regressions"
  command: "uv run -- python -m pytest -m fast -q && uv run -- python -m pytest tests/unit/generators/test_tpcds_generator.py
    tests/unit/test_fractional_scale_factors.py tests/unit/core/test_tpcds_resource_resolution.py tests/unit/core/visualization/test_post_run_summary.py
    tests/unit/mcp/test_visualization_tools.py tests/e2e/test_error_handling.py -q"
  expected_output: "fast suite and targeted TPC-DS, result-pipeline, visualization, and MCP tests pass"

files_affected:
  modified:
  - "benchbox/core/benchmark_registry.py"
  - "benchbox/core/tpcds/"
  - "benchbox/core/results/"
  - "benchbox/core/validation/"
  - "benchbox/core/visualization/"
  - "benchbox/cli/commands/run.py"
  - "benchbox/cli/commands/datagen.py"
  - "benchbox/cli/commands/submit.py"
  - "benchbox/cli/commands/publish.py"
  - "benchbox/cli/commands/report.py"
  - "benchbox/cli/display.py"
  - "benchbox/cli/output.py"
  - "benchbox/mcp/tools/"
  - "tests/unit/"
  - "tests/unit/core/visualization/"
  - "tests/unit/mcp/"
  - "tests/e2e/"
  - "_sources/"
  - "CLAUDE.md"
  - "GEMINI.md"
  - "AGENTS.md"
  added:
  - "_sources/tpcds-subscale-probe.md"
  - "_sources/adr/"
  - "_project/TODO/main/active/support-unofficial-tpcds-scales-with-methodology-guardrails.yaml"

scope_limit:
  only_modify:
  - "benchbox/core/benchmark_registry.py"
  - "benchbox/core/tpcds/"
  - "benchbox/core/results/"
  - "benchbox/core/validation/"
  - "benchbox/core/visualization/"
  - "benchbox/cli/commands/run.py"
  - "benchbox/cli/commands/datagen.py"
  - "benchbox/cli/commands/submit.py"
  - "benchbox/cli/commands/publish.py"
  - "benchbox/cli/commands/report.py"
  - "benchbox/cli/display.py"
  - "benchbox/cli/output.py"
  - "benchbox/mcp/tools/"
  - "tests/unit/"
  - "tests/unit/core/visualization/"
  - "tests/unit/mcp/"
  - "tests/e2e/"
  - "_sources/"
  - "CLAUDE.md"
  - "GEMINI.md"
  - "AGENTS.md"
  - "_project/TODO/"
  do_not_modify:
  - "benchbox/core/tpch/"
  - "benchbox/platforms/"
  - "results-data/"
  - "results-explorer/"

context_sections:
  "Key Findings": |
    The current codebase does not merely prefer official TPC-DS scales. It
    explicitly documents that the native generator crashes below 1.0 in the
    benchmark registry, benchmark runner, generator manager, and tests. The
    existing `examples/data/tpcds_sf001/` directory is only a placeholder, not
    usable bundled sample data.

    The wider system also assumes comparability too early: result building,
    schema export, CLI summaries, ranking queries, publication metadata, and
    report import do not model unofficial-vs-official TPC-DS as a first-class
    distinction.
  "Decision Boundaries": |
    This item is not approved to merge until Gate Zero documents whether native
    subscale generation is real and what the resulting table contract is. If the
    probe shows a mixed-platform outcome, continue with per-platform capability
    gating and a user-supplied-only fallback on failing platforms. If the probe
    disproves the premise on all bundled binaries, implementation stops and the
    follow-up path is handled as separate work.

technical_requirements:
  constraints:
  - "Do not merge behavior changes before Gate Zero is documented."
  - "Treat mixed-platform Gate Zero results as a per-platform capability decision, not as an all-or-nothing enable/disable."
  - "Keep official comparable TPC-DS behavior unchanged for existing workflows."
  - "Carry methodology/comparability through export, import, storage, ranking, and publication."
  - "Preserve existing scale token formatting; add an unofficial marker instead of a new token scheme."

success_metrics:
- "BenchBox can only produce unofficial TPC-DS scale artifacts after explicit user opt-in and after the native-generation
  path is empirically validated."
- "Unofficial TPC-DS runs are durably marked and isolated across JSON export, import, database storage, publication metadata,
  and historical rankings."
- "Official TPC metrics are suppressed or rendered non-comparable for unofficial TPC-DS runs, while official TPC-DS workflows
  remain unchanged."
- "Validation behavior for unofficial subscale runs matches the documented table contract derived from Gate Zero."

open_questions:
- "For unofficial subscale runs, which TPC-DS tables are mandatory, optional-empty, or expected to be absent according to
  actual generator output?"
- "Should plain non-official `sf>=1` TPC-DS runs remain allowed by default outside `--official`, or should the new opt-in
  cover all non-official scales?"

research_value:
  hypothesis: |
    BenchBox can support unofficial TPC-DS scales for local developer workflows
    without corrupting official comparability, but only if the native data path
    is proven viable and the resulting methodology is carried through the full
    result/report/storage pipeline as a first-class distinction.
  validation_approach: |
    Use an empirical binary probe to establish feasibility, then implement the
    minimal cross-cutting changes required to persist and enforce that
    distinction across generation, validation, export, import, ranking, and
    publication.
  success_criteria: |
    The feature is considered proven only when the empirical probe is published,
    the result pipeline preserves methodology/comparability end-to-end, and no
    report, publication, or ranking surface can silently present unofficial
    TPC-DS runs as official comparable cohorts.
