id: uat-clickhouse-server-loader-type-coverage
title: "Fix ClickHouse-server client-insert type coverage exposed by corpus UAT"
worktree: main
priority: High
status: In Progress
category: Platform Expansion

description: |
  After PR #738 fixed the clickhouse-server provisioning/auth/database/load
  path, the 2026-06-01 corpus rerun no longer showed the original 57/57
  platform wipeout. The largest remaining failure cluster is server-mode
  client-side INSERT type conversion during load, not generic ClickHouse
  compatibility.

  PR #738 introduced this path because Docker-backed ClickHouse server cannot
  read arbitrary host files through server-side `file()`. The path is
  BenchBox-side and gated to server deployment mode; clickhouse-local keeps the
  faster `file()` load path. The review of the corpus found no silent
  corruption: empty strings into String columns are preserved as empty strings,
  and every observed type gap fails loudly during load.

  Corpus evidence:
    - Matrix: `~/Developer/benchmark_runs/logs/uat_clickhouse_server_rc1_corpus_20260601/matrix_summary.tsv`
    - Cells: `~/Developer/benchmark_runs/logs/uat_clickhouse_server_rc1_corpus_20260601/cells.jsonl`

  Representative failures:
    - `clickhouse-server_tpcds_0.01_20260601_120433.log`: `Code: 53. Type mismatch in VALUES section... Column c_current_cdemo_sk: required argument is not an integer`; TPC-DS then reports many empty tables.
    - `clickhouse-server_clickbench_1.0_20260601_122208.log`: `ClickHouse file loading failed: 'str' object has no attribute 'tzinfo'`.
    - `clickhouse-server_vector_search_0.01_20260601_133631.log`: `ClickHouse file loading failed: could not convert string to float: '[0.0206561,...]'`.
    - `clickhouse-server_datavault_0.01_20260601_133545.log`: `ClickHouse file loading failed: argument of type 'Table' is not iterable`.

  Observed impact from the corpus classifier: roughly 22 of the 42 non-passing
  cells are loader type coverage. Bucket A is nullable numeric/date handling
  for empty fields into non-Nullable ClickHouse columns (TPC-DS, NYC Taxi,
  FlightData, plus downstream TPC-DS OBT). Bucket B is DATETIME/TIMESTAMP
  conversion, currently falling through as strings and producing the `tzinfo`
  error (ClickBench, H2O DB, TSBS). Bucket C is array/vector parsing for
  Vector Search. Bucket D is Arrow/Table-valued schema introspection for
  DataVault. These are not platform lifecycle failures: Docker up/down
  succeeded and the corpus executed all 57 cells.

prior_art:
  - "_project/TODO/main/planning/uat-clickhouse-server-rc1-recovery.yaml: records the fixed platform wipeout and corpus artifact paths"
  - "benchbox/platforms/base/data_loading.py: shared data-loading helpers and server-mode insertion path"
  - "benchbox/platforms/clickhouse/: ClickHouse adapter, metadata, setup, and shared local/server behavior"
  - "benchbox/utils/compression_mixin.py: existing file handling patterns that should remain untouched"

work:
  - id: w0
    summary: "Re-derive the four loader buckets against current develop and pin compact evidence"
    status: done
    notes: |
      Before changing code, drive the conversion/introspection helpers directly
      where possible and preserve a compact command/result summary in this
      TODO. Raw stdout belongs in `/tmp` or `BENCHBOX_OUTPUT_DIR`, not git.
      Expected signatures to re-confirm: empty numeric/date handling, raw
      DATETIME/TIMESTAMP string fallthrough, bracketed array/vector parsing,
      and Table-valued schema introspection.

      RE-VALIDATION (2026-07-16, helper boundary, no Docker). The conversion
      helper is benchbox/platforms/base/data_loading.py:
      ClickHouseNativeHandler._convert_field_for_clickhouse (landed in the Jul-10
      import ded0a94b, AFTER this TODO's 2026-06-01 corpus evidence — the
      "Identified" status was stale). Driving the helper directly:
        - Bucket A (empty numeric/date -> NULL): ALREADY IMPLEMENTED and matches
          the ratified policy. `if value == "": return None if
          needs_non_string_value else value`; empty String stays "". No change
          needed.
        - Bucket B (DATETIME/TIMESTAMP): BROKEN. "DATETIME".startswith("DATE") is
          True so `date.fromisoformat("2020-01-15 12:30:00")` raised; bare
          "TIMESTAMP" fell through as str -> driver 'str' has no 'tzinfo'.
        - Bucket C (array/vector): PARTIAL. "Array(...)" parsed, but DuckDB
          "FLOAT[128]" matched the FLOAT branch -> float("[...]") raised.
        - Bucket D (Table-valued schema introspection): NOT re-reproduced here;
          needs the live DataVault schema object. `_get_column_type_names`
          returns [] for non-dict schemas (silent no-conversion) rather than the
          observed `'Table' is not iterable`, so the crash site is elsewhere in
          the DataVault load path. Deferred (see deferred + w2 notes).
  - id: w1
    summary: "Add focused unit coverage for ClickHouse-server value conversion"
    needs: [w0]
    status: done
    notes: |
      Cover nullable integer fields with blank/null-like inputs, Date and
      DateTime/DateTime64 values supplied as strings, ClickHouse arrays such
      as `Array(Float32)` from bracketed literals, and schemas represented by
      benchmark `Table` objects instead of simple dicts.

      DONE for Buckets A/B/C in tests/unit/platforms/test_clickhouse_adapter.py:
      test_empty_numeric_date_fields_become_null_not_default (A),
      test_datetime_and_timestamp_conversion (B),
      test_vector_bracket_type_parsed_to_list_not_float (C). The Table-object
      schema case (Bucket D) is NOT covered yet — it depends on the Bucket D fix
      site, which is deferred.
  - id: w2
    summary: "Fix the client-batch conversion path without regressing clickhouse-local"
    needs: [w1]
    status: in_progress
    notes: |
      Keep server mode on client-side INSERT batches for Docker-host files;
      do not revert to server-side `file()` loading. Preserve shared ClickHouse
      SQL behavior while making type conversion explicit for server inserts.
      Implement the ratified nullable-empty-field policy (see open_questions
      below): an empty numeric/date value becomes NULL with Nullable(T) DDL
      where the reference schema marks the column nullable; NEVER coerce to a
      type default. An empty landing in a column the reference schema marks
      NOT NULL must fail loudly as a benchmark data defect, not be silently
      defaulted.

      DONE (Buckets B + C): _convert_field_for_clickhouse now classifies
      DATETIME/DateTime64/TIMESTAMP before the DATE prefix and parses to Python
      datetime; DuckDB bracketed vector/list types (FLOAT[128], INTEGER[]) route
      to array parsing via the new _CLICKHOUSE_VECTOR_TYPE_RE. Bucket A was
      already correct (nullable-empty -> NULL, ratified policy) and is unchanged.
      All at the helper boundary per anti_patterns; clickhouse-local path
      untouched (server-mode-only helper).
      REMAINING (Bucket D): Table-valued schema introspection for DataVault is
      NOT fixed — its crash site needs live repro (see deferred). w2 stays
      in_progress until D lands or is split into its own item.
  - id: w3
    summary: "Verify representative failing cells live against ClickHouse server"
    needs: [w2]
    status: pending
    notes: |
      Use UAT-managed Docker and the same benchmark output root. Representative
      cells should include TPC-DS SF=0.01, one DateTime-heavy cell, DataVault
      SF=0.01, and Vector Search SF=0.01 before broadening to the corpus.

      NOT RUN in the 2026-07-16 session: no Docker daemon in the remote
      container, so live cells could not execute. Buckets A/B/C are proven at
      the unit/helper boundary only; a live run of the TPC-DS, DateTime-heavy,
      and Vector Search cells is still required to close this. DataVault stays
      blocked on the Bucket D fix.

      Code-review checkpoints to confirm on the live run (from the w2 review):
        - tz-AWARE datetime round-trip: a source timestamp with a 'Z'/offset
          (e.g. TSBS RFC3339) parses to a tz-aware Python datetime;
          clickhouse-driver converts it to the column timezone. Verify the
          stored instant is NOT shifted vs the naive-string benchmarks (a
          non-UTC DateTime column would otherwise corrupt time aggregates). If a
          shift is observed, normalise to naive-UTC before insert.
        - Non-ISO/epoch DateTime formats: datetime.fromisoformat parses only
          ISO-8601, so epoch-seconds or locale timestamps raise (loud, per
          guardrails). Confirm the actual ClickBench/TSBS/H2O timestamp formats
          are ISO; if any emit epoch/other, extend the parser (tracked in
          deferred).

deferred:
  - summary: "Bucket D: harden Table-valued schema introspection for DataVault"
    reason: "The observed `'Table' is not iterable` crash is outside _convert_field_for_clickhouse (which safely no-ops on non-dict schemas); pinning the real crash site needs a live DataVault load, unavailable this session. Split candidate if it grows."
  - summary: "Bucket B follow-up: non-ISO / epoch-integer DateTime source formats"
    reason: "datetime.fromisoformat covers the observed ISO forms; epoch-seconds or locale formats, if any benchmark emits them, would raise (loud, per guardrails) and can be handled when a real case appears."

deps:
  needs:
    - uat-clickhouse-server-rc1-recovery
    - uat-clickhouse-loader-empty-value-nullability

must_preserve:
  - "Keep the PR #738 server-mode contract: Docker-host data files load through client-side batches, not server-side `file()` paths."
  - "Do not make clickhouse-local depend on clickhouse-driver server-only conversion behavior."
  - "Empty strings into String columns must remain empty strings; do not turn them into NULL."
  - "Do not silently coerce invalid data to zero or empty strings; nullability and parse failures must stay visible."
  - "The 15 passing corpus cells must keep passing: TPCH, SSB, AMPLab, AI primitives, and TPCH skew at all three scales."

approach: |
  Treat this as a typed ingestion problem in BenchBox's ClickHouse-server
  client-insert path. Use the corpus logs to select the minimal set of
  conversion cases, add targeted tests around the conversion and
  schema-introspection helpers, then prove with live representative cells. Fix
  DATETIME/TIMESTAMP ordering before DATE-prefix handling, add array parsing,
  and harden schema introspection for non-dict schemas. If a benchmark has
  invalid source data rather than a loader gap, split that into a
  benchmark-specific TODO instead of hiding it in the adapter.

anti_patterns:
  - "DO NOT restore server-side `file()` loading for Docker-backed clickhouse-server; it cannot read arbitrary host paths."
  - "DO NOT fix only the first TPC-DS integer column; prove the conversion policy at the helper boundary."
  - "DO NOT paper over DataVault by special-casing a benchmark name if the bug is schema-object introspection."
  - "DO NOT route DATETIME/TIMESTAMP through date-only parsing; preserve the time component."
  - "DO NOT fabricate default numeric/date values for fields that are genuinely nullable."

verification:
  - description: "Focused ClickHouse loader conversion tests pass"
    command: "uv run -- python -m pytest tests/unit/platforms/test_clickhouse_adapter.py -q"
    expected_output: "tests pass, including nullable integer, datetime, array, and Table-schema conversion cases"
  - description: "TPC-DS small cell loads instead of failing on integer conversion"
    command: "BENCHBOX_OUTPUT_DIR=~/Developer/benchmark_runs make uat-cell PLATFORM=clickhouse-server BENCHMARK=tpcds SCALE=0.01"
    expected_output: "cell reaches query execution or passes; it must not fail with `required argument is not an integer` during load"
  - description: "DateTime-heavy representative cell loads"
    command: "BENCHBOX_OUTPUT_DIR=~/Developer/benchmark_runs make uat-cell PLATFORM=clickhouse-server BENCHMARK=h2odb SCALE=0.01"
    expected_output: "cell does not fail with `'str' object has no attribute 'tzinfo'` during load"
  - description: "Vector Search small cell parses vector arrays"
    command: "BENCHBOX_OUTPUT_DIR=~/Developer/benchmark_runs make uat-cell PLATFORM=clickhouse-server BENCHMARK=vector_search SCALE=0.01"
    expected_output: "cell does not fail converting bracketed vector values to scalar floats"
  - description: "DataVault small cell handles benchmark Table schemas"
    command: "BENCHBOX_OUTPUT_DIR=~/Developer/benchmark_runs make uat-cell PLATFORM=clickhouse-server BENCHMARK=datavault SCALE=0.01"
    expected_output: "cell does not fail with `argument of type 'Table' is not iterable` during load"

scope_limit:
  only_modify:
    - "benchbox/platforms/base/data_loading.py"
    - "benchbox/platforms/clickhouse/"
    - "tests/unit/platforms/test_clickhouse_adapter.py"
    - "_project/TODO/main/planning/uat-clickhouse-server-loader-type-coverage.yaml"

success_metrics:
  - "Representative loader failures are covered by unit tests before the implementation fix."
  - "TPC-DS, a DateTime-heavy benchmark, DataVault, and Vector Search each progress past load on clickhouse-server."
  - "NYC Taxi and FlightData progress past the nullable numeric/date load failures."
  - "No regression to the passing TPCH, SSB, AMPLab, AI primitives, and TPCH skew clickhouse-server cells."
  - "No silent String corruption: empty String fields remain empty strings."

open_questions:
  - question: "For Bucket A, should empty numeric/date values drive Nullable(...) DDL or default coercion for non-Nullable columns?"
    gating: false
    resolved: true
    affects: ["w2", "success_metrics"]
    decision: "RESOLVED 2026-07-16 (repo owner, via [[uat-clickhouse-loader-empty-value-nullability]]): empty numeric/date -> NULL with Nullable(T) DDL where the reference schema marks the column nullable; do NOT coerce to a type default; fail loudly on empty-into-genuinely-NOT-NULL (treat as a benchmark data defect). Grounded in this benchmark's own schema (c_current_cdemo_sk/hdemo_sk/addr_sk and s_rec_start/end_date are nullable:true) and this item's must_preserve/anti_patterns. w2 implements this policy; the code change carries CODEOWNERS review."

metadata:
  tags: [clickhouse-server, uat, loader, type-conversion]
  estimated_effort: "Large"
  created_date: "2026-06-01"
  last_updated: "2026-06-01T16:15:33-04:00"
