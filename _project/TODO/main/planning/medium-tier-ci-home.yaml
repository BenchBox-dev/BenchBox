id: medium-tier-ci-home
title: "Give the medium pytest speed tier a CI ring (currently local-only for ~81 files)"
worktree: main
priority: Medium
status: Not Started
category: Testing and Quality Assurance

description: |
  81 test files carry `@pytest.mark.medium` (confirmed via
  `grep -rl pytest.mark.medium tests/ | wc -l`), and `make test-medium`
  (Makefile:90-91) is the only place that runs the full medium tier:
  `-m "medium and not (slow or stress or resource_heavy or live_integration)"`.
  No CI workflow invokes this Make target or an equivalent selector across
  the full tier. The develop-PR gate's `code-test` job explicitly documents
  that the fast-lane selection at pr.yml:450 (`-m "fast and not (...)"`)
  skips medium-marked tests, and only pin-points two specific medium nodes
  by name as explicit exceptions (pr.yml:419-424
  `test_cross_surface_gates_are_pinned_in_make_and_ci`, and pr.yml:438-445
  `test_plan_capture_phase.py`) - the comment at pr.yml:419 states this
  outright: "Medium-marked ratchet, so the fast-lane selection below skips
  it; invoke the node explicitly". `test.yml` (main-only, test.yml:3-9) also
  does not select the medium marker as a tier - it has its own fast-lane
  test job and separate `integration` job, neither of which is `-m medium`.
  So the ~79 other medium-marked files run only when a developer runs
  `make test-medium` locally; nothing in CI exercises them as a tier.

work:
  - id: w1
    summary: "Measure the medium lane's full runtime"
    status: pending
    notes: |
      Run `time make test-medium` and record wall-clock time and pass/fail
      count. This number drives w2's placement choice - a lane under a few
      minutes is a candidate for a path-filtered PR job; a lane of 10+
      minutes is a better fit for nightly or post-merge.

  - id: w2
    summary: "Place the medium lane in one CI ring based on w1's measurement"
    needs: [w1]
    status: pending
    notes: |
      Three placement options, pick one based on w1:
        - develop-post-merge.yml: add a `medium-test` job calling
          `make test-medium`, non-blocking to the PR but caught
          immediately post-merge via the existing auto-revert path
          (mirrors the `lint`/`fast-test` jobs, develop-post-merge.yml:20-56).
        - nightly.yml matrix: add `make test-medium` as a job/step alongside
          the existing `integration-smoke` matrix.
        - path-filtered PR job in pr.yml: only viable if w1's runtime is
          fast-lane-comparable; otherwise this adds meaningful PR latency
          to every code-touching PR since the medium tier is not narrowly
          scoped to one path group.
      Default to develop-post-merge.yml unless w1's runtime data argues
      otherwise - it is the option most consistent with keeping the PR gate
      fast while still catching regressions within one merge.

  - id: w3
    summary: "If placed post-merge, add medium-test to the Makefile ci-test auto-revert mirror"
    needs: [w2]
    status: pending
    notes: |
      Only applies if w2 chose develop-post-merge.yml. `make test-medium`
      is already Makefile:90-91 and needs no new `ci-`-prefixed wrapper
      (unlike `ci-test`, which mirrors a workflow-specific fast-test
      invocation) - call it directly from the new develop-post-merge.yml
      job, same shape as the existing `lint`/`fast-test` jobs.

  - id: w4
    summary: "Add a lint/unit check asserting every speed-marker tier has at least one CI consumer"
    needs: [w2]
    status: pending
    notes: |
      Find the right home in `tests/unit/test_standardized_test_commands.py`
      (which already has Makefile/CI-pinning assertions, e.g.
      `test_cross_surface_gates_are_pinned_in_make_and_ci` and
      `test_makefile_test_medium_uses_five_workers` at :271-278) or
      `tests/unit/test_marker_strategy.py` (which already asserts marker
      hygiene, e.g. `test_tree_has_no_fast_or_medium_decorators`,
      `test_routine_test_modules_have_a_single_top_level_speed_marker`) -
      whichever file's existing scope is the closer fit. Add a test that
      greps the known speed markers (fast, medium, slow, stress,
      resource_heavy, live_integration) against the CI workflow YAMLs
      (pr.yml, test.yml, nightly.yml, develop-post-merge.yml) and fails if
      any marker has zero references across all four files. This is the
      guardrail that prevents this exact gap (a tier silently losing every
      CI consumer) from recurring undetected.

deps:
  needs: []

must_preserve:
  - "make test-medium's existing selector and worker count (`-n 5`, Makefile:90-91, asserted by test_makefile_test_medium_uses_five_workers) is unchanged - CI calls the same target, it does not reimplement the selector."
  - "The two explicit medium-node pins already in pr.yml (pr.yml:419-424, :438-445) stay in place even after the full tier gets a home - they are documented as deliberate early-promotion exceptions (pr.yml comments), not superseded by a general medium ring."
  - "pr.yml's fast-lane PR gate keeps its current latency profile - do not fold the full medium tier into the required code-test job."

approach: |
  Measure first (w1), then place (w2) - do not default to a placement
  before the runtime data exists. Reuse develop-post-merge.yml's existing
  job pattern (checkout, setup-python, setup-uv, sync, run) rather than
  inventing new plumbing. The w4 guardrail is the highest-leverage unit
  here since it prevents recurrence of exactly this class of gap for any
  future marker tier, not just medium - implement it regardless of which
  placement w2 picks.

anti_patterns:
  - "DO NOT add `-m medium` selection into the existing required code-test job in pr.yml - because that inflates every develop PR's latency by the full tier's runtime regardless of what changed - use a separate job/ring."
  - "DO NOT remove or generalize away the two explicit medium-node pins at pr.yml:419-424/:438-445 as part of giving medium a general home - because their comments explain they were promoted early for specific regression reasons (#901, #909) - leave them as belt-and-suspenders duplication, not redundant work to clean up."
  - "DO NOT write the w4 guardrail as a hardcoded list of today's marker-to-workflow mappings that never re-derives from the workflow files - because that check would pass even after this exact gap reappears for a new marker - it must actually parse/grep the current workflow YAMLs and current marker set at test time."

verification:
  - description: "Baseline runtime measured before placement decision"
    command: "time make test-medium"
    expected_output: "wall-clock time and pass count recorded and cited in the PR/commit implementing w2"
  - description: "Medium tier runs somewhere in CI post-implementation"
    command: "gh run list --workflow=develop-post-merge.yml --limit=1 --json databaseId --jq '.[0].databaseId' | xargs -I{} gh api repos/{owner}/{repo}/actions/runs/{}/jobs --jq '.jobs[].name'"
    expected_output: "job list includes the new medium-test (or equivalent) job name, depending on w2's placement"
  - description: "New marker-tier-has-a-CI-consumer guardrail fails when a tier is unwired"
    command: "uv run -- python -m pytest tests/unit/test_standardized_test_commands.py -k marker_tier_has_ci_consumer -v"
    expected_output: "test exists and passes on the post-w2/w4 tree; temporarily commenting out the medium job in the CI workflow makes it fail"

scope_limit:
  only_modify:
    - ".github/workflows/develop-post-merge.yml"
    - ".github/workflows/nightly.yml"
    - ".github/workflows/pr.yml"
    - "Makefile"
    - "tests/unit/test_standardized_test_commands.py"
    - "tests/unit/test_marker_strategy.py"
  do_not_modify:
    - ".github/workflows/test.yml"
    - "tests/conftest.py"

prior_art:
  - "Makefile:90-91 test-medium target — reuse as-is"
  - "develop-post-merge.yml:20-56 lint/fast-test job pattern — reuse as the likely placement pattern"
  - "pr.yml:419-424, :438-445 explicit medium-node pin comments — reuse the '#issue-number promoted this specific regression' documentation style for any new guardrail comments"
  - "tests/unit/test_standardized_test_commands.py:271-287 test_makefile_test_medium_uses_five_workers / test_medium_marker_policy_is_documented_as_explicit_routing — reuse as the closest existing precedent for the w4 guardrail's shape"

metadata:
  owners:
    - joeharris76
  tags:
    - ci
    - pytest
    - medium
    - test-markers
    - develop-pr-gate
  estimated_effort: Medium
  created_date: "2026-07-03"

impact:
  business_value: |
    81 test files stop being effectively untested by CI - regressions in
    the medium tier are currently only caught if a developer happens to
    run `make test-medium` locally before pushing.
  technical_debt: |
    Adds the w4 guardrail so no future speed-marker tier can silently lose
    every CI consumer the way medium did, without a test noticing.

success_metrics:
  - "The medium tier runs in at least one CI ring on a defined cadence (per-PR path-filtered, post-merge, or nightly)."
  - "A regression introduced only in medium-marked tests is caught by CI within one merge-to-develop cycle, not only when a developer runs make test-medium locally."
  - "tests/unit/test_standardized_test_commands.py or test_marker_strategy.py has a passing test that fails if any speed-marker tier loses its last CI consumer."

open_questions:
  - question: "Should the medium lane land in develop-post-merge.yml, nightly.yml, or a path-filtered pr.yml job?"
    gating: true
    affects: ["w2 placement decision"]
    default: "develop-post-merge.yml (non-blocking, caught within one merge cycle) unless w1's runtime makes post-merge concurrency-group serialization (cancel-in-progress: false) a bottleneck."
