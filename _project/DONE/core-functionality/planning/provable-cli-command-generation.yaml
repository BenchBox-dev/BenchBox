# Provably complete CLI command generation for dry-run and interactive preview
#
# Location: _project/TODO/core-functionality/planning/provable-cli-command-generation.yaml

id: provable-cli-command-generation
title: "Make generate_cli_command provably complete - all behavior-affecting CLI params round-trip"
worktree: core-functionality
priority: High
status: Completed
completed_date: "2026-03-05"
category: Core Functionality
description: |
  generate_cli_command() in benchbox/cli/dryrun.py reconstructs a CLI command string
  from individual parameters. It is used by:
  1. display_interactive_preview() - "Equivalent CLI command:" shown before wizard execution
  2. DryRunDisplay - schema/query preview output

  The function is structurally unsound: the parameter list is manually maintained and
  has fallen out of sync with the Click run command. An adversarial review identified:

  - 7 behavior-affecting CLI params not represented at all (--platform-option,
    --plan-config, --presort, --sorted-ingestion-mode, --sorted-ingestion-method,
    --databricks-clustering-strategy, --liquid-clustering-columns, --global-cache)
  - 1 lossy rendering bug (--compression zstd silently dropped because of incorrect
    default-skip logic)
  - 1 incomplete rendering (--table-format doesn't include compression suffix like
    delta:zstd)
  - No structural guarantee that new CLI params added in the future will be reflected
    here - the two lists (Click decorators vs generate_cli_command params) are
    completely disconnected

  The fix has two parts:
  A. Add all missing params and fix rendering bugs
  B. Add a completeness test that introspects the Click command and asserts parity

metadata:
  tags:
    - cli
    - dry-run
    - interactive-wizard
    - correctness
  estimated_effort: Medium
  created_date: "2026-03-05"

impact:
  business_value: |
    The "Equivalent CLI command" is often copy-pasted by users to automate benchmarks
    in CI/CD. If it silently drops config (e.g., driver_version, sorted ingestion,
    plan capture config), the automated run produces different results than the
    interactive one, leading to confusing benchmark regressions.
  user_impact: |
    Users trust the displayed command to be a faithful reproduction. Silent omissions
    violate that trust and waste debugging time.

files_affected:
  modified:
    - benchbox/cli/dryrun.py
    - benchbox/cli/commands/run.py
    - tests/unit/cli/test_cli_dryrun.py
    - tests/unit/cli/test_interactive_dry_run.py

must_preserve:
  - "generate_cli_command() continues to omit default values (e.g., --scale 0.01, --table-mode native, --tuning notuning) so
    the output stays readable"
  - "display_interactive_preview() preview table (non-CLI-command portion) continues rendering correctly with current formatting"
  - "All existing tests in test_cli_dryrun.py and test_interactive_dry_run.py continue passing"

approach: |
  Part A - Add missing params and fix bugs:

  1. Add these parameters to generate_cli_command() signature, _VALUE_PARAMS list,
     and rendering logic:
     - platform_options: dict[str, str] | None - render as repeated --platform-option KEY=VALUE
     - plan_config: str | None - render as --plan-config VALUE
     - presort: str | None - render as --presort VALUE
     - sorted_ingestion_mode: str | None - render as --sorted-ingestion-mode VALUE (skip "off")
     - sorted_ingestion_method: str | None - render as --sorted-ingestion-method VALUE
     - databricks_clustering_strategy: str | None - render as --databricks-clustering-strategy VALUE
     - liquid_clustering_columns: str | None - render as --liquid-clustering-columns VALUE
     - global_cache: bool - render as --global-cache flag

  2. Thread these through display_interactive_preview() signature and body, adding
     them to both the preview table and the generate_cli_command() call.

  3. Thread them through the production call site in run.py:2399.

  4. Fix compression rendering: currently `compression_str if compression_str != "zstd" else None`
     drops explicit --compression zstd. Instead, only skip when compression was NOT
     explicitly set by the user. The caller (display_interactive_preview) should pass
     compression=None when compress_data is False, and pass the full compression string
     otherwise (never skip based on value).

  5. Fix table_format rendering: when table_format_compression is set and differs from
     the default ("snappy"), render as --table-format FORMAT:COMPRESSION (e.g.,
     --table-format delta:zstd). This matches the CLI's accepted input format.

  Part B - Add completeness test:

  6. Create a test that introspects the Click run command's parameter list, identifies
     all behavior-affecting params (exclude --dry-run, --verbose, --quiet,
     --non-interactive, --no-monitoring, --no-progress, --ignore-memory-warnings,
     --output), and asserts that generate_cli_command() accepts a corresponding kwarg
     for each one. This test breaks whenever a new CLI param is added without updating
     generate_cli_command().

  The introspection test uses click.testing or direct inspection of the command's
  params attribute. The set of "cosmetic-only" params is maintained as a frozenset
  in the test - any new param not in the frozenset and not in generate_cli_command
  will cause a test failure with a clear message.

anti_patterns:
  - "DO NOT use **kwargs in generate_cli_command - because it hides missing params at call sites - use explicit typed parameters
    so IDE/linter catches omissions"
  - "DO NOT reconstruct the command from BenchmarkConfig.options dict - because that dict is a grab-bag with non-CLI keys (unified_tuning_configuration,
    df_tuning_config, etc.) - use explicit params"
  - "DO NOT skip compression based on its value - because the default depends on config files and env vars - skip only when
    the user didn't set it (compress_data=False)"

verification:
  - description: "All existing CLI dry-run tests pass"
    command: "uv run -- python -m pytest tests/unit/cli/test_cli_dryrun.py tests/unit/cli/test_interactive_dry_run.py -v --override-ini='addopts='"
    expected_output: "all tests pass, 0 failures"
  - description: "Completeness test catches missing params"
    command: "uv run -- python -m pytest tests/unit/cli/test_cli_dryrun.py -k completeness -v --override-ini='addopts='"
    expected_output: "1 passed"
  - description: "Generate command with all params renders correctly"
    command: "uv run -- python -m pytest tests/unit/cli/test_cli_dryrun.py -k 'all_params' -v --override-ini='addopts='"
    expected_output: "tests pass, output contains all flags"
  - description: "Lint passes"
    command: "uv run ruff check benchbox/cli/dryrun.py benchbox/cli/commands/run.py"
    expected_output: "All checks passed"

scope_limit:
  only_modify:
    - benchbox/cli/dryrun.py
    - benchbox/cli/commands/run.py
    - tests/unit/cli/test_cli_dryrun.py
    - tests/unit/cli/test_interactive_dry_run.py

work:
  - id: w1
    summary: "Add 8 missing params to generate_cli_command() signature and rendering logic"
    status: done
    notes:
      "platform_options, plan_config, presort, sorted_ingestion_mode, sorted_ingestion_method, databricks_clustering_strategy,
      liquid_clustering_columns, global_cache"

  - id: w2
    summary: "Fix compression rendering - remove the lossy `!= 'zstd'` skip, pass None when compress_data=False instead"
    status: done

  - id: w3
    summary:
      "Fix table_format rendering - include compression suffix (e.g., delta:zstd) when table_format_compression differs
      from 'snappy' default"
    status: done

  - id: w4
    summary:
      "Thread new params through display_interactive_preview() signature, preview table rows, and internal generate_cli_command()
      call"
    needs: [w1, w2, w3]
    status: done

  - id: w5
    summary:
      "Thread new params through the production call site in run.py:2399 (display_interactive_preview call in interactive
      wizard)"
    needs: [w4]
    status: done

  - id: w6
    summary: "Add unit tests for each new param in generate_cli_command (renders when set, omitted when default)"
    needs: [w1]
    status: done

  - id: w7
    summary:
      "Add completeness introspection test: introspect Click run command params, assert parity with generate_cli_command
      kwargs"
    status: done
    notes: |
      Test structure:
      - Get Click command object for `run`
      - Extract all param names (excluding known cosmetic ones)
      - Get generate_cli_command's inspect.signature().parameters
      - Assert CLI_PARAMS - COSMETIC_PARAMS ⊆ GENERATE_PARAMS
      - Cosmetic frozenset: {dry_run, verbose, quiet, non_interactive,
        no_monitoring, no_progress, ignore_memory_warnings, output}

  - id: w8
    summary:
      "Add integration test: display_interactive_preview with all params set, verify CLI command output contains every
      flag"
    needs: [w5]
    status: done

deferred:
  - summary: "Refactor to derive CLI command from resolved config objects instead of individual params"
    reason:
      "Larger architectural change - the param-threading approach works for now and is testable via the completeness test.
      The introspection test (w7) provides the structural guarantee that was missing."
  - summary: "Add round-trip test: parse generated CLI command back through Click and verify config equivalence"
    reason: "Would be the ultimate correctness proof but requires building a CLI command parser, which is significant scope"
