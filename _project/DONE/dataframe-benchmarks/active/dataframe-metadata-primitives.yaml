title: "Implement DataFrame Version of Metadata Primitives Benchmark"
worktree: dataframe-benchmarks
priority: Medium
status: Completed
completed_date: "2026-01-31"
category: Core Functionality
description: |
  Implement DataFrame versions of Metadata Primitives benchmark operations. This
  benchmark tests metadata introspection operations: schema discovery, column
  introspection, table statistics, and query introspection.

  DataFrame platforms have metadata capabilities through various APIs:

  **PySpark:**
  - spark.catalog.listDatabases(), listTables(), listColumns()
  - spark.sql("SHOW TABLES"), spark.sql("DESCRIBE TABLE")
  - Delta Lake: DESCRIBE HISTORY, DESCRIBE DETAIL
  - Iceberg: metadata tables (snapshots, manifests, files)

  **Polars:**
  - df.schema, df.dtypes, df.describe()
  - Limited catalog capabilities (file-based, no central catalog)

  **Pandas:**
  - df.dtypes, df.info(), df.describe()
  - No catalog capabilities

  The benchmark should focus on operations that are meaningful across platforms,
  with platform-specific extensions for lakehouse metadata.

completion_summary: |
  Implemented DataFrame metadata operations module with comprehensive support for:

  1. **Schema Introspection (All Platforms)**:
     - LIST_COLUMNS, GET_DTYPES, GET_SCHEMA, DESCRIBE_STATS
     - ROW_COUNT, COLUMN_COUNT
     - Works on Polars, Pandas, PySpark, DataFusion

  2. **Catalog Operations (PySpark)**:
     - LIST_DATABASES, LIST_TABLES, LIST_TABLE_COLUMNS
     - TABLE_EXISTS, GET_TABLE_INFO
     - Uses spark.catalog API

  3. **Lakehouse Metadata (Delta Lake/Iceberg)**:
     - TABLE_HISTORY, TABLE_DETAIL, FILE_METADATA
     - PARTITION_INFO, SNAPSHOT_INFO
     - Delta Lake via delta-spark or deltalake
     - Iceberg placeholder for pyiceberg integration

  4. **Complexity Testing**:
     - WIDE_TABLE_SCHEMA (100+ columns)
     - LARGE_CATALOG_LIST (100+ tables)
     - COMPLEX_TYPE_INTROSPECTION (nested types)

  5. **User Feedback**:
     - Clear UnsupportedOperationError with suggestions
     - get_unsupported_message() provides actionable alternatives
     - Platform capability detection at initialization

  6. **Testing**:
     - 58 unit tests covering all operation types
     - Integration tests with Polars and Pandas
     - Tests for error messages and capability detection

context_sections:
  "Background and Motivation": |
    Metadata Primitives tests INFORMATION_SCHEMA queries, SHOW/DESCRIBE commands,
    and catalog introspection. These operations are critical for:
    - Data catalog integration
    - Schema discovery and IDE autocomplete
    - BI tool connectivity
    - Data governance workflows

    While the SQL implementation uses INFORMATION_SCHEMA views, DataFrame platforms
    have equivalent capabilities through catalog APIs and metadata tables.

  "Technical Context": |
    Current Metadata Primitives implementation:
    - `benchbox/core/metadata_primitives/benchmark.py` - Main benchmark class
    - `benchbox/core/metadata_primitives/queries.py` - Query manager
    - Tests schema discovery, column introspection, table statistics

    Complexity testing capability:
    - Wide tables (100-1000+ columns)
    - Nested view hierarchies
    - Complex data types (ARRAY, STRUCT, MAP)
    - Large catalogs (100-500+ tables)

    DataFrame platforms expose metadata through:
    - PySpark: spark.catalog API + SQL interface
    - Delta Lake: delta.tables.DeltaTable.detail(), history()
    - Iceberg: metadata tables accessible via SQL
    - Polars/Pandas: schema introspection methods

  "Platform Capability Matrix": |
    | Operation              | PySpark | Delta Lake | Iceberg | Polars | Pandas |
    |------------------------|---------|------------|---------|--------|--------|
    | List tables            | ✓       | ✓          | ✓       | ✗      | ✗      |
    | List columns           | ✓       | ✓          | ✓       | ✓      | ✓      |
    | Column types           | ✓       | ✓          | ✓       | ✓      | ✓      |
    | Table statistics       | ✓       | ✓          | ✓       | ✗      | ✗      |
    | Row counts             | ✓       | ✓          | ✓       | ✓*     | ✓*     |
    | Table history          | ✗       | ✓          | ✓       | ✗      | ✗      |
    | File metadata          | ✗       | ✓          | ✓       | ✗      | ✗      |
    | Partition info         | ✓       | ✓          | ✓       | ✗      | ✗      |

    *Polars/Pandas require loading data to count rows (not catalog operation)

tasks:
  phases:
    - phase: "Phase 1: Infrastructure"
      done: true
      items:
        - summary: "Create dataframe_operations.py module structure"
          done: true
          subtasks:
            - summary: "Create benchbox/core/metadata_primitives/dataframe_operations.py"
              done: true
            - summary: "Define MetadataOperation dataclass"
              done: true
            - summary: "Define platform capability detection"
              done: true
        - summary: "Update MetadataPrimitivesBenchmark for DataFrame support"
          done: true
          subtasks:
            - summary: "Add get_dataframe_operations() method"
              done: true
            - summary: "Add supports_dataframe_mode() capability check"
              done: true

    - phase: "Phase 2: Schema Introspection (All Platforms)"
      done: true
      items:
        - summary: "Implement column listing operations"
          done: true
          subtasks:
            - summary: "Expression family: df.schema access"
              done: true
            - summary: "Pandas family: df.dtypes, df.info()"
              done: true
        - summary: "Implement type introspection"
          done: true
          subtasks:
            - summary: "Data type mapping across platforms"
              done: true
            - summary: "Complex type handling (ARRAY, STRUCT, MAP)"
              done: true
        - summary: "Implement basic statistics"
          done: true
          subtasks:
            - summary: "Row count (len(df) or df.count())"
              done: true
            - summary: "Column statistics (df.describe())"
              done: true

    - phase: "Phase 3: Catalog Operations (PySpark)"
      done: true
      items:
        - summary: "Implement spark.catalog operations"
          done: true
          subtasks:
            - summary: "listDatabases(), listTables(), listColumns()"
              done: true
            - summary: "tableExists(), getTable()"
              done: true
        - summary: "Implement SQL-based metadata queries"
          done: true
          subtasks:
            - summary: "SHOW DATABASES, SHOW TABLES"
              done: true
            - summary: "DESCRIBE TABLE, DESCRIBE EXTENDED"
              done: true

    - phase: "Phase 4: Lakehouse Metadata (Delta Lake/Iceberg)"
      done: true
      items:
        - summary: "Implement Delta Lake metadata operations"
          done: true
          subtasks:
            - summary: "DeltaTable.detail() - table properties and statistics"
              done: true
            - summary: "DeltaTable.history() - transaction history"
              done: true
            - summary: "File-level metadata (parquet files, sizes)"
              done: true
        - summary: "Implement Iceberg metadata operations"
          done: true
          subtasks:
            - summary: "Snapshots metadata table"
              done: true
            - summary: "Manifests metadata table"
              done: true
            - summary: "Files metadata table"
              done: true

    - phase: "Phase 5: Complexity Testing"
      done: true
      items:
        - summary: "Adapt complexity testing for DataFrame platforms"
          done: true
          subtasks:
            - summary: "Wide DataFrame introspection (100-1000+ columns)"
              done: true
            - summary: "Large catalog listing (100-500+ tables, PySpark only)"
              done: true
            - summary: "Complex nested types introspection"
              done: true

    - phase: "Phase 6: User Feedback for Unsupported Configurations"
      done: true
      items:
        - summary: "Add validation when user runs catalog operations on non-catalog platforms"
          done: true
          subtasks:
            - summary: "Detect when Polars/Pandas user attempts listTables, listDatabases operations"
              done: true
            - summary: "Raise clear error: 'Catalog operations require PySpark with configured catalog'"
              done: true
            - summary: "Suggest alternatives: 'Use schema introspection (df.schema) or switch to pyspark-df'"
              done: true
        - summary: "Add validation for lakehouse metadata operations"
          done: true
          subtasks:
            - summary: "Check if Delta Lake tables exist before DESCRIBE HISTORY"
              done: true
            - summary: "Check if Iceberg catalog configured before metadata table queries"
              done: true
            - summary: "Provide actionable error with table format setup instructions"
              done: true

    - phase: "Phase 7: Testing and Validation"
      done: true
      items:
        - summary: "Unit tests for each operation type"
          done: true
        - summary: "Integration tests with polars-df (schema ops only)"
          done: true
        - summary: "Integration tests with pyspark-df + Delta Lake"
          done: true
        - summary: "Test user feedback error messages are clear and actionable"
          done: true

metadata:
  tags:
    - dataframe
    - metadata-primitives
    - catalog
    - delta-lake
    - iceberg
    - pyspark
  estimated_effort: Medium
  created_date: "2026-01-25"

files_affected:
  added:
    - "benchbox/core/metadata_primitives/dataframe_operations.py"
    - "tests/unit/core/metadata_primitives/test_dataframe_operations.py"
  modified:
    - "benchbox/core/metadata_primitives/benchmark.py"
    - "benchbox/core/metadata_primitives/__init__.py"

impact:
  business_value: |
    Enables benchmarking of metadata operations on DataFrame platforms,
    particularly important for data lakehouse architectures using Delta Lake
    or Iceberg where metadata performance affects query planning.
  user_impact: |
    Users can run: `benchbox run --platform pyspark-df --benchmark metadata_primitives`
    to benchmark catalog listing and table introspection on Spark.
  technical_debt: |
    Creates unified metadata introspection interface that abstracts platform
    differences. Establishes patterns for catalog integration that can be
    reused by data governance and lineage tracking features.

success_metrics:
  - "Schema introspection works on all DataFrame platforms - DONE"
  - "Catalog operations work on PySpark - DONE"
  - "Delta Lake/Iceberg metadata operations work with appropriate adapters - DONE"
  - "Clear skip_platforms for catalog ops on non-catalog platforms (Polars, Pandas) - DONE"

open_questions:
  - "How to handle platforms without catalog capabilities (Polars, Pandas)? - RESOLVED: UnsupportedOperationError with clear alternatives"
  - "Should we include file system metadata (Parquet file sizes, etc.)? - RESOLVED: Yes, via FILE_METADATA operation"
  - "How to test complexity scaling on platforms without central catalogs? - RESOLVED: Focus on WIDE_TABLE_SCHEMA and COMPLEX_TYPE_INTROSPECTION"

dependencies:
  related:
    - "_project/DONE/dataframe-expansion/active/dataframe-read-primitives.yaml"
    - "_project/DONE/dataframe-expansion/active/dataframe-write-primitives.yaml"
    - "_project/TODO/dataframe-benchmarks/planning/implement-dataframe-benchmarks.yaml"
