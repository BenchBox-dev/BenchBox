id: interactive-wizard-validation-mode
title: "Add --validation mode selection to interactive wizard"
worktree: ux-improvements
priority: Medium
status: Completed
description: |
  The CLI supports `--validation exact|loose|range|disabled|full` for result
  validation configuration. The interactive wizard doesn't expose this option.

  Validation modes:
  - exact: Strict result matching
  - loose: Tolerant matching for floating point
  - range: Accept results within expected range
  - disabled: Skip validation
  - full: All validation checks enabled

category: User Experience

tasks:
  phases:
  - phase: "Phase 1: Implementation"
    done: false
    items:
    - summary: "Add validation mode selection in advanced tuning"
      done: false
    - summary: "Explain validation mode implications"
      done: false
    - summary: "Wire to validation config"
      done: false

  - phase: "Phase 2: Testing"
    done: false
    items:
    - summary: "Test each validation mode works correctly"
      done: false

metadata:
  tags:
  - cli
  - ux
  - interactive
  - validation
  estimated_effort: "Small"
  created_date: "2025-12-21"

impact:
  user_impact: |
    Enables validation control:
    - Disable validation for speed during development
    - Strict validation for production benchmarks
    - Loose validation for cross-platform comparisons

files_affected:
  modified:
  - "benchbox/cli/tuning.py"
  - "benchbox/cli/commands/run.py"

success_metrics:
- "Validation mode selectable in wizard"
- "Mode correctly applied to benchmark execution"
