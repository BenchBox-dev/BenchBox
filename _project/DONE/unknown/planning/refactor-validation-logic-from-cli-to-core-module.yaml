id: refactor-validation-logic-from-cli-to-core-module
title: ✅ Refactor Validation Logic from CLI to Core Module
category: Pre-Release High Priority Tasks (COMPLETED)
sections:
  Notes: '- "**Priority**: High"

    - "**Status**: Completed ✅"

    - "**Description**: Finished moving validation orchestration into the core runtime, exposing a reusable `ValidationService`
    while keeping the CLI as a presentation layer."

    - "**Context**: Validation hooks were split between CLI and core, limiting programmatic use and forcing adapters to manage
    their own engines."

    - "**Tasks**:"

    - "**Files Affected**:"

    - "**Success Metrics**:"'
  2025 CLI Hardening: '- "**Tasks (2025 update)**:"

    - "**Testing**: Full `uv run -- python -m pytest` currently fails in this workspace because of pre-existing integration/unit
    test failures (benchmark fixtures clashing with `pytest-benchmark`). Unit test sweep (`tests/unit`) also inherits the
    same baseline issues."

    - "**Impact**: The CLI is now a pure presentation layer; validation reporting flows entirely from the core lifecycle,
    and users can control manifest validation without touching CLI internals."

    - New unit tests cover service orchestration and CLI delegation paths.

    - "**Impact**: HIGH - Establishes validation as a first-class core capability, simplifying adapter responsibilities and
    enabling programmatic workflows outside the CLI."'
worktree: unknown
priority: Medium
status: Completed
description: '- "**Priority**: High"

  - "**Status**: Completed ✅"

  - "**Description**: Finished moving validation orchestration into the core runtime, exposing a reusable `ValidationService`
  while keeping the CLI as a presentation layer."

  - "**Context**: Validation hooks were split between CLI and core, limiting programmatic use and forcing adapters to manage
  their own engines."

  - "**Tasks**:"

  - "**Files Affected**:"

  - "**Success Metrics**:"'
