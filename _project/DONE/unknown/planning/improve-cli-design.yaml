id: improve-cli-design
title: ✅ Improve CLI Design
category: Pre-Release High Priority Tasks (COMPLETED)
sections:
  Notes: '- "**Priority**: High"

    - "**Status**: Completed ✅"

    - "**Description**: Remaining CLI issues focus on cleaning up platform-specific options, hardcoded database wiring, and
    brittle quiet-mode behavior after recent orchestration refactors."

    - "**Context**: `benchbox run` still declares ClickHouse flags directly, database selection uses hardcoded branches, and
    quiet mode relies on ad-hoc console rebindings across modules."

    - "**Tasks**:"

    - "**Files Affected**:"

    - "**Success Metrics**:"

    - "**Impact**: HIGH - Unlocks cleaner extension patterns and resolves long-standing CLI UX gaps"'
worktree: unknown
priority: Medium
status: Completed
description: '- "**Priority**: High"

  - "**Status**: Completed ✅"

  - "**Description**: Remaining CLI issues focus on cleaning up platform-specific options, hardcoded database wiring, and
  brittle quiet-mode behavior after recent orchestration refactors."

  - "**Context**: `benchbox run` still declares ClickHouse flags directly, database selection uses hardcoded branches, and
  quiet mode relies on ad-hoc console rebindings across modules."

  - "**Tasks**:"

  - "**Files Affected**:"

  - "**Success Metrics**:"

  - "**Impact**: HIGH - Unlocks cleaner extension patterns and resolves long-standing CLI UX gaps"'
