id: fix-critical-verbosity-harmonization-issues-post-implementation
title: ✅ Fix Critical Verbosity Harmonization Issues (Post-Implementation)
category: Pre-Release High Priority Tasks (Should-Fix Before Release)
status: Completed
priority: High
impact: Restores a coherent verbosity pipeline across the CLI, orchestrator, and TPC-H stack so quiet/verbose modes behave
  predictably and expose structured logging hooks for downstream adapters.
sections:
  Summary: '"Reworked the shared `VerbosityMixin` API, migrated TPC-H generators/streams off ad-hoc `print()` calls, enforced
    CLI flag validation, and delivered integration coverage that exercises the full CLI → orchestrator → adapter flow for
    verbose and quiet operation."'
  Completed Tasks: '- Verbosity architecture

    - TPC-H migration

    - CLI UX hardening

    - Testing'
  Files Affected: '- benchbox/base.py

    - benchbox/platforms/base.py

    - benchbox/platforms/base/adapter.py

    - benchbox/utils/verbosity.py

    - benchbox/cli/commands/run.py

    - benchbox/core/tpch/generator.py

    - benchbox/core/tpch/streams.py

    - tests/unit/cli/test_cli_verbose.py

    - tests/integration/test_tpch_verbosity_flow.py

    - _project/PROJECT_TODO.yaml'
sections_done:
- name: Verification
  text: '"- \"`uv run -- python -m pytest tests/unit/cli/test_cli_verbose.py tests/integration/test_tpch_verbosity_flow.py`\"

    - \"`uv run -- python -m pytest` *(fails: pre-existing ImportError for `_resolve_tpcds_tool_and_template_paths` in `tests/unit/core/test_tpcds_resource_resolution.py`.)*\"

    "'
worktree: unknown
description: '"Reworked the shared `VerbosityMixin` API, migrated TPC-H generators/streams off ad-hoc `print()` calls, enforced
  CLI flag validation, and delivered integration coverage that exercises the full CLI → orchestrator → adapter flow for verbose
  and quiet operation."'
