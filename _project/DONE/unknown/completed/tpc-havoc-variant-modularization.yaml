id: tpc-havoc-variant-modularization
title: ✅ TPC-Havoc Variant Modularization
category: Code Quality and Technical Debt
status: Completed
priority: Medium
impact: Makes the 220 TPC-Havoc query variants manageable by moving them into per-query modules with shared utilities, simplifying
  future edits and targeted testing.
sections:
  Summary: '"Replaced the monolithic `benchbox/core/tpchavoc/variants.py` with a modular package (`variant_base`, `variant_sets`,
    lightweight registry), refreshed documentation, and introduced focused unit tests mirroring the new structure."'
  Completed Tasks: '- Responsibility audit

    - Code extraction

    - Testing and verification'
  Testing: '"Unable to run `uv run -- python -m pytest` because the sandbox cannot reach PyPI to install pytest; please run
    the suite locally."'
worktree: unknown
description: '"Replaced the monolithic `benchbox/core/tpchavoc/variants.py` with a modular package (`variant_base`, `variant_sets`,
  lightweight registry), refreshed documentation, and introduced focused unit tests mirroring the new structure."'
