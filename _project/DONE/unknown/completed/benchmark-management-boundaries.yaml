id: benchmark-management-boundaries
title: ✅ Benchmark Management Boundaries
category: Project Lifecycle Management
status: Completed
impact: Eliminates duplication and tight coupling; keeps CLI as presentation-only for benchmark operations.
sections:
  Notes: '- "CLI execution pipeline now calls `benchbox.core.benchmark_loader.get_benchmark_instance` instead of importing/core
    class construction."

    - "Added `tests/unit/cli/test_cli_benchmark_boundaries.py` to assert delegation to core loader."'
extra_fields:
  Description: Ensured programmatic benchmark loading is centralized in core while keeping interactive selection in CLI.
worktree: unknown
priority: Medium
description: '- "CLI execution pipeline now calls `benchbox.core.benchmark_loader.get_benchmark_instance` instead of importing/core
  class construction."

  - "Added `tests/unit/cli/test_cli_benchmark_boundaries.py` to assert delegation to core loader."'
