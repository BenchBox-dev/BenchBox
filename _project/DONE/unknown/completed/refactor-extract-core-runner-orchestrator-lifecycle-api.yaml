id: refactor-extract-core-runner-orchestrator-lifecycle-api
title: '"✅ Refactor: Extract Core Runner/Orchestrator (Lifecycle API)"'
category: Core Architecture
status: Completed
impact: The lifecycle is now available programmatically, unifying behavior across CLI and runner and reducing duplication
  while maintaining simple test patch points.
sections:
  Notes: '- "Added `tests/unit/core/test_runner_lifecycle.py` covering data_only, load_only, and standard paths."

    - "Updated `tests/unit/cli/test_cli_power_throughput.py` to assert adapter kwargs rather than internal run_config objects."'
extra_fields:
  Description: Moved benchmark lifecycle/orchestration (generate → optional validate → load → execute) from CLI internals
    into a reusable core API. The CLI now delegates to core while remaining a presentation layer.
worktree: unknown
priority: Medium
description: '- "Added `tests/unit/core/test_runner_lifecycle.py` covering data_only, load_only, and standard paths."

  - "Updated `tests/unit/cli/test_cli_power_throughput.py` to assert adapter kwargs rather than internal run_config objects."'
