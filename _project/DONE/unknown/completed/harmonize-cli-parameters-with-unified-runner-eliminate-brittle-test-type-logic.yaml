id: harmonize-cli-parameters-with-unified-runner-eliminate-brittle-test-type-logic
title: ✅ Harmonize CLI Parameters with unified_runner (Eliminate Brittle test_type Logic)
category: Pre-Release High Priority Tasks (COMPLETED)
status: Completed
priority: High
impact: Eliminated fragile logic causing runtime errors, created consistent parameter experience across CLI and unified_runner,
  simplified CLI maintenance
sections:
  Description: '"Successfully replaced fragile CLI parameter inference logic with unified approach matching `unified_runner`,
    eliminating brittle `test_type` conditionals and creating consistent parameter structure across all interfaces."'
  Completed Tasks: '- "✅ **Replace CLI Orchestrator Logic with Core Runner**: Eliminated custom CLI orchestrator phase handling"

    - "✅ **Unify Parameter Names**: Aligned CLI parameters with unified_runner conventions"

    - "✅ **Adopt LifecyclePhases as Single Source of Truth**: Eliminated dual phase systems"

    - "✅ **Simplify Configuration Flow**: Used shared configuration utilities"

    - "✅ **Update All CLI Examples and Documentation**: Reflected parameter changes"

    - "✅ **Test Integration**: Verified unified approach works correctly"'
  Technical Results: '- "**Eliminated Brittle Logic**: No more complex `test_type` inference logic with fragile conditionals"

    - "**Parameter Consistency**: CLI and unified_runner now use identical parameter patterns (`--phases`)"

    - "**Robust Phase Handling**: Consistent and robust phase handling across all interfaces"

    - "**Simplified Codebase**: Removed fragile conditionals and variable scoping issues"

    - "**Maintainable CLI**: CLI now acts as thin presentation layer over core functionality"'
  Files Modified: '- "**Modified**: `benchbox/cli/main.py` (changed `--phase` to `--phases`, updated help text)"

    - "**Refactored**: `benchbox/cli/orchestrator.py` (replaced complex logic with `run_benchmark_lifecycle` usage)"

    - "**Updated**: `CLAUDE.md` (updated quick commands to use `--phases`)"

    - "**Updated**: All CLI help text and examples (parameter name changes)"

    - "**Updated**: `tests/unit/cli/test_cli_parameter_consistency.py` and related test files (updated test parameter names)"

    - "**Updated**: `docs/USAGE.md`, `docs/usage/cli-quick-start.md`, `docs/usage/dry-run.md` (parameter documentation changes)"'
worktree: unknown
description: '"Successfully replaced fragile CLI parameter inference logic with unified approach matching `unified_runner`,
  eliminating brittle `test_type` conditionals and creating consistent parameter structure across all interfaces."'
