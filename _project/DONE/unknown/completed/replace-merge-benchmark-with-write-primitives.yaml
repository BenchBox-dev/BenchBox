id: replace-merge-benchmark-with-write-primitives
title: ✅ Replace Merge Benchmark with Write Primitives
category: Pre-Release High Priority Tasks (Should-Fix Before Release)
status: Completed
priority: High
impact: Retires the brittle Merge benchmark in favour of a reliable write-focused workload that exercises insert/update/delete/merge
  flows, aligns exporter schema metadata, and equips the CLI with a production-ready alternative.
sections:
  Summary: Designed and implemented the Write Primitives benchmark on top of TPC-H data, harmonised CLI exposure/documentation,
    upgraded the result schema to v1.1 for iteration metadata, and added integration coverage to ensure adapters execute the
    new workload predictably.
  Completed Tasks: '- "Research & Planning"

    - Benchmark Implementation

    - "Platform & Schema Integration"

    - "Testing & Validation"'
  Success Metrics: '- ✅ Write Primitives benchmark available via benchbox CLI/API with documented operations.

    - ✅ Result exports carry iteration/run_type metadata under schema version 1.1.

    - ✅ Integration test ensures adapters can execute write + verify sequences end-to-end (skipped when MERGE unsupported).

    - ✅ Legacy Merge benchmark code, docs, and tests removed in favour of the new workload.'
worktree: unknown
description: Designed and implemented the Write Primitives benchmark on top of TPC-H data, harmonised CLI exposure/documentation,
  upgraded the result schema to v1.1 for iteration metadata, and added integration coverage to ensure adapters execute the
  new workload predictably.
