id: canonicalize-result-schema-2
title: ✅ Canonicalize Result Schema
category: Pre-Release High Priority Tasks (Should-Fix Before Release)
status: Completed
priority: High
impact: Establishes a single JSON export contract for BenchBox ahead of release.
sections:
  Summary: Unified CLI and core exporters on schema v1.0, added canonical validation helpers, and documented the contract
    for downstream consumers.
  Completed Tasks: '- ✅ Export pipeline

    - "✅ Tests & docs"

    - ✅ Tooling updates'
  Testing: '"`UV_CACHE_DIR=.uv-cache uv run -- python -m pytest`

    *(fails: pre-existing ImportError in tests/unit/core/test_tpcds_resource_resolution.py for `_resolve_tpcds_tool_and_template_paths`.)*

    "'
  Files Affected: '- benchbox/core/results/exporter.py

    - benchbox/core/results/schema.py (new)

    - benchbox/cli/output.py

    - tests/unit/cli/test_json_optimization.py'
worktree: unknown
description: Unified CLI and core exporters on schema v1.0, added canonical validation helpers, and documented the contract
  for downstream consumers.
