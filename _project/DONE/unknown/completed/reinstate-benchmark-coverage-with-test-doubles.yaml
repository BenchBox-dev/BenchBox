id: reinstate-benchmark-coverage-with-test-doubles
title: ✅ Reinstate Benchmark Coverage with Test Doubles
category: Test Infrastructure
status: Completed
priority: High
impact: Restores coverage for the core TPCH/TPCDS benchmarks without relying on compiled binaries or optional toolchains.
sections:
  Summary: Replaced runtime-heavy dependencies with hermetic fakes during tests so the benchmark orchestration code is executed
    under coverage again.
  Completed Tasks: '- ✅ Fake TPCH components

    - ✅ Fake TPCDS components

    - ✅ Coverage configuration'
  Testing: '"- \"`uv run -- python -m pytest tests/unit/core/test_tpch_benchmark_fakes.py`\"

    - \"`uv run -- python -m pytest tests/unit/core/test_tpcds_benchmark_fakes.py`\"

    - \"`uv run -- python -m pytest` (fails due to missing optional TPC-DI dependencies such as pandas)\"

    "'
  Files Affected: '- tests/unit/core/test_tpch_benchmark_fakes.py (new)

    - tests/unit/core/test_tpcds_benchmark_fakes.py (new)

    - pytest-ci.ini'
worktree: unknown
description: Replaced runtime-heavy dependencies with hermetic fakes during tests so the benchmark orchestration code is executed
  under coverage again.
