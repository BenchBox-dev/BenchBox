id: uat-timeout-enforcement-hardening
title: "UAT: harden per-cell timeout enforcement for process trees"
worktree: uat-enabled-platforms-remediation
priority: High
status: Completed
category: Testing and Quality Assurance

description: |
  The 2026-05-12 enabled-platform UAT config set `per_cell_timeout_s: 600`, but
  the run notes indicate timeout enforcement did not reliably stop long cells.
  The artifact matrix reported no timed-out cells even though the full command
  ran for 1036.4s and execution did not cover the full enumerated matrix.

  Current unit coverage exercises a simple child process timeout. The UAT cell
  path runs `uv run -- benchbox ...`, which can create a larger process tree and
  buffered stdout/stderr behavior. Add targeted regression coverage and harden
  timeout handling so a per-cell timeout kills the whole cell process tree and
  is reported unambiguously as timed out.

work:
  - id: w0
    summary: "Reproduce timeout behavior with a benchbox-like process tree"
    status: done
    notes: |
      Build a local synthetic command that mimics `uv run -- benchbox` spawning
      descendants and sleeping beyond a tiny timeout. Record a compact summary
      of process tree, command, exit status, and whether descendants survived.

      Completed with fast regression coverage in
      `tests/uat/test_timeouts.py`: a parent process spawns a sleeping
      grandchild, the wrapper times out at 1s, returns exit 124, and the child
      PID is gone before the assertion completes.

  - id: w1
    summary: "Add process-tree timeout regression tests"
    needs: [w0]
    status: done
    notes: |
      Extend timeout/runner tests beyond a one-process `time.sleep`. Cover a
      child that spawns a grandchild, a command that writes output, and the
      `run_cell()` status mapping. Tests must be deterministic and fast.

      Added process-tree, noisy-pipe, and `run_cell()` timeout marker tests.
      The timeout/runner verification command passed: 21 tests.

  - id: w2
    summary: "Harden timeout kill and reporting semantics"
    needs: [w1]
    status: done
    notes: |
      Ensure timeout handling terminates the process group or process tree,
      escalates if needed, drains output safely, and returns a stable timeout
      code/status that matrix and report phases classify as timed out rather
      than failed or missing.

      `run_with_timeout()` now uses `communicate(timeout=...)` so stdout/stderr
      pipes are drained while the timeout is enforced. Timeout classification
      remains stable at exit 124 with `timed_out=True`, and `run_cell()` writes
      a compact `UAT_TIMEOUT` marker into the cell log.

  - id: w3
    summary: "Expose timeout accounting in matrix and report outputs"
    needs: [w2]
    status: done
    notes: |
      Make timed-out cells visible in `cells.jsonl`, `matrix_summary.tsv`, and
      reports. Include enough command/status context to debug future timeouts
      without committing raw logs.

      `cells.jsonl` now includes a boolean `timed_out` field. Existing report
      output already counted timed-out cells separately; the synthetic timeout
      smoke confirmed `failed=0 timed_out=1`.

deferred:
  - summary: "Global runtime scheduler redesign"
    reason: "The immediate defect is per-cell kill/report behavior; platform-level scheduling policy is separate."

prior_art:
  - "tests/uat/timeouts.py:29-89 -- `run_with_timeout()` currently uses `subprocess.Popen`, process groups, and timeout handling"
  - "tests/uat/runner.py:123-166 -- `run_cell()` invokes `benchbox_run_argv()` and maps timeout status into `CellResult`"
  - "tests/uat/test_timeouts.py -- existing unit tests cover simple timeout behavior but not benchbox-like process trees"
  - "tests/uat/phases/execute.py -- execution phase consumes timeout settings and aggregates cell results"
  - "/Users/joe/Developer/benchmark_runs/logs/uat_enabled_today_platforms_20260512/uat-enabled-today-platforms-20260512.yaml -- observed config with `per_cell_timeout_s: 600`"

must_preserve:
  - "Timeout tests remain fast and do not require live Docker or cloud credentials"
  - "A timed-out cell cannot leave child processes running after the test or UAT phase exits"
  - "Non-timeout failures retain their original return code and stderr context"

approach: |
  Reproduce the failure shape locally before changing kill logic. Prefer a
  small, portable subprocess-tree fixture in tests over relying on a slow real
  benchmark. Keep report changes additive so existing parsers can still read old
  fields.

anti_patterns:
  - "DO NOT infer the exact timeout root cause solely from the 2026-05-12 artifacts; write a reproducer first"
  - "DO NOT add sleeps long enough to slow the fast suite materially"
  - "DO NOT treat timeouts as ordinary failed cells in summaries"

verification:
  - description: "Timeout and runner unit coverage passes"
    command: "uv run -- python -m pytest tests/uat/test_timeouts.py tests/uat/test_runner.py -m fast -q"
    expected_output: "passes with process-tree timeout coverage"
  - description: "Targeted UAT timeout smoke records a timed-out cell"
    command: "BENCHBOX_OUTPUT_DIR=~/Developer/benchmark_runs make uat-sweep CONFIG=tests/uat/configs/uat-synthetic-timeout-smoke.yaml"
    expected_output: "`cells.jsonl` and `matrix_summary.tsv` mark the synthetic cell as timed out with no surviving child process"
    actual_output: |
      Expected nonzero sweep exit because the execute phase contained a timed
      out cell. Run directory:
      `/Users/joe/Developer/benchmark_runs/logs/uat_synthetic_timeout_smoke_20260512`.
      `cells.jsonl` recorded `status: timed-out`, `timed_out: true`, and
      `exit_code: 124`. `matrix_summary.tsv` recorded
      `rows=1 passed=0 failed=0 timed_out=1`.
  - description: "Existing UAT matrix coverage still passes"
    command: "uv run -- python -m pytest tests/uat/test_matrix.py tests/uat/test_phases.py -m fast -q"
    expected_output: "passes"

scope_limit:
  only_modify:
    - "tests/uat/timeouts.py"
    - "tests/uat/runner.py"
    - "tests/uat/orchestrator.py"
    - "tests/uat/phases/execute.py"
    - "tests/uat/phases/report.py"
    - "tests/uat/test_timeouts.py"
    - "tests/uat/test_runner.py"
    - "tests/uat/test_phases.py"
    - "tests/uat/test_orchestrator.py"
    - "tests/uat/configs/"
    - "docs/operations/uat-framework.md"
  do_not_modify:
    - "benchbox/platforms/"
    - "benchbox/core/"
    - "benchmark_runs/"

metadata:
  owners:
    - joeharris76
  tags:
    - uat
    - timeout
    - process-tree
    - reporting
  estimated_effort: Medium
  created_date: "2026-05-12"
  last_updated: "2026-05-12T00:00:00"

impact:
  business_value: |
    Prevents UAT sweeps from hanging or masking timeouts as ordinary failures.
  user_impact: |
    Maintainers get bounded, explainable UAT runs with reliable failure
    classification.
  technical_debt: |
    Adds missing coverage for the actual subprocess shape used by UAT cells.

success_metrics:
  - "A synthetic benchbox-like timeout kills all descendants and reports `timed_out`"
  - "Matrix and report outputs include timeout counts"
  - "Fast UAT timeout tests remain deterministic"
