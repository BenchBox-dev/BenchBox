id: result-object-migration-benchmarkresult-benchmarkresults-2
title: ✅ Result Object Migration (BenchmarkResult → BenchmarkResults)
worktree: result-object-migration
category: Pre-Release High Priority Tasks (Should-Fix Before Release)
status: Completed
priority: High
impact: '"Unified all result generation on the lifecycle-aware `BenchmarkResults` model, simplifying exporter logic and removing
  the brittle CLI-only dataclass."'
sections:
  Summary: '"Removed the legacy `benchbox.core.config.BenchmarkResult` class, rewired CLI execution/error paths and the data-only
    pipeline to produce `BenchmarkResults`, and updated schema/exporter utilities, documentation, and tests to expect the
    canonical model exclusively."'
  Key Changes: '- "Deleted the legacy result dataclass from `benchbox/core/config.py` and refreshed CLI helpers/tests to consume
    `BenchmarkResults` via shared fixtures."

    - "Simplified `benchbox.core.results.schema.build_canonical_payload()` and the exporter to require `BenchmarkResults`,
    adding negative tests for unsupported types."

    - "Authored `docs/migration/result_object_migration.md`, refreshed the schema reference, and recorded the breaking change
    in the changelog."'
  Testing: '"uv run -- python -m pytest  # fails on existing repository issues: missing `benchbox.Merge` export and TPC-DS
    generator modules referenced by other pending worktrees

    "'
description: '"Removed the legacy `benchbox.core.config.BenchmarkResult` class, rewired CLI execution/error paths and the
  data-only pipeline to produce `BenchmarkResults`, and updated schema/exporter utilities, documentation, and tests to expect
  the canonical model exclusively."'
