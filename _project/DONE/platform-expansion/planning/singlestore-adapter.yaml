id: singlestore-adapter
title: "Add SingleStore Platform Adapter"
worktree: platform-expansion
priority: Medium
status: Completed
category: Platform Expansion
description: |
  Add support for SingleStore (formerly MemSQL), a distributed SQL database designed
  for real-time analytics and transactions with both row and column storage.

  SingleStore is notable for:
  - Real-time analytics with sub-second query latency
  - Distributed architecture with horizontal scaling
  - MySQL wire protocol compatibility
  - Native Python SDK with 10x faster data reading than PyMySQL
  - Cloud (Helios) and self-managed deployment options

  Reference: https://docs.singlestore.com/

context_sections:
  "Technical Background": |
    SingleStore uses MySQL wire protocol but has significant extensions for:
    - Columnstore tables (optimized for analytics)
    - Rowstore tables (optimized for transactions)
    - Hash and reference tables for distributed joins
    - Pipeline ingestion for streaming data

    The official Python SDK (singlestoredb) provides:
    - DB-API 2.0 compliance
    - C extension for 10x faster reads than PyMySQL
    - Support for both Helios (cloud) and self-managed

  "Connection Options": |
    1. singlestoredb (official, recommended) - pip install singlestoredb
    2. PyMySQL (pure Python, slower)
    3. mysql-connector-python
    4. ODBC driver

  "Implementation Pattern": |
    Follow the Doris adapter pattern (benchbox/platforms/doris.py) — standalone adapter
    using MySQL wire protocol, not a PostgreSQLAdapter subclass. Key parallels:
    - PyMySQL-style connectivity (but using singlestoredb SDK instead)
    - Stream/bulk loading via platform-native mechanism (LOAD DATA LOCAL INFILE)
    - Columnstore DDL with distribution keys (similar to Doris DISTRIBUTED BY HASH)
    - SQLGlot dialect: use 'mysql' or test if 'singlestoredb' is available

metadata:
  completed_date: "2026-04-04"
  tags:
  - platform
  - sql
  - mysql-compatible
  - distributed
  - olap
  - cloud
  - singlestore
  estimated_effort: Medium
  created_date: "2026-01-26"

technical_requirements:
  dependencies:
  - "singlestoredb (official Python SDK)"
  compatibility: |
    SingleStore supports MySQL wire protocol with extensions.
    Requires SingleStore-specific DDL for optimal performance (columnstore, shard keys).

files_affected:
  added:
  - "benchbox/platforms/singlestore.py"
  - "benchbox/platforms/credentials/singlestore.py"
  - "tests/unit/platforms/test_singlestore.py"
  - "docs/platforms/singlestore.md"
  modified:
  - "benchbox/platforms/__init__.py"
  - "docs/platforms/index.md"
  - "docs/platforms/cloud-platforms.rst"
  - "docs/platforms/sql-platforms.rst"
  - "docs/platforms/comparison-matrix.md"

must_preserve:
- "All existing platform adapter tests pass: uv run -- python -m pytest tests/unit/platforms/ -v --tb=short"
- "Platform registry autodiscovery continues working for all existing platforms"
- "No new required dependencies added to core install (singlestoredb is optional)"

anti_patterns:
- "DO NOT modify existing adapter files — because SingleStore is additive — create a new singlestore.py file"
- "DO NOT use PyMySQL — because SingleStore's native singlestoredb SDK is significantly faster — use the singlestoredb package"
- "DO NOT use rowstore tables for TPC fact tables — because columnstore is dramatically faster for analytical queries — default
  to columnstore for all benchmark tables"

verification:
- description: "New adapter imports correctly"
  command: "uv run -- python -c \"from benchbox.platforms.singlestore import SingleStoreAdapter; print('import OK')\""
  expected_output: "import OK"
- description: "Unit tests pass"
  command: "uv run -- python -m pytest tests/unit/platforms/test_singlestore.py -v --tb=short"
  expected_output: "all tests pass, 0 failures"
- description: "Existing platform tests unaffected"
  command: "uv run -- python -m pytest tests/unit/platforms/ -v --tb=short"
  expected_output: "all tests pass, 0 failures"

scope_limit:
  only_modify:
  - "benchbox/platforms/singlestore.py"
  - "benchbox/platforms/credentials/singlestore.py"
  - "benchbox/platforms/__init__.py"
  - "tests/unit/platforms/test_singlestore.py"
  - "docs/platforms/singlestore.md"
  - "docs/platforms/index.md"
  - "docs/platforms/cloud-platforms.rst"
  - "docs/platforms/sql-platforms.rst"
  - "docs/platforms/comparison-matrix.md"
  do_not_modify:
  - "benchbox/platforms/clickhouse_cloud.py"
  - "benchbox/platforms/doris.py"
  - "benchbox/core/runner.py"

success_metrics:
- "SingleStore adapter passes all TPC-H queries at SF 1"
- "SingleStore adapter passes all TPC-DS queries at SF 1"
- "Columnstore tables used for fact tables"
- "Bulk loading achieves >100K rows/sec"
- "Documentation covers both Helios and self-managed"

deferred:
- id: d1
  summary: "Pipeline ingestion support for streaming scenarios"
  reason: "Streaming ingest is a niche use case; core adapter should stabilize first"
  condition: "Core adapter stable and passing all TPC benchmarks; user demand for streaming ingest"
- id: d2
  summary: "Advanced shard key optimization per benchmark schema"
  reason: "Default shard keys may be sufficient; optimization only justified after profiling"
  condition: "After initial shard key strategy validated at SF 1+; performance profiling shows join bottlenecks"

work:
# --- Research phase ---
- id: w1
  summary: "Research SingleStore SQL extensions, columnstore vs rowstore, and bulk loading options"
  status: done
- id: w2
  summary: "Research distributed join patterns, shard key strategies, and Helios vs self-managed differences"
  status: done

# --- Core adapter ---
- id: w3
  summary: "Create SingleStoreAdapter skeleton class with platform registration metadata"
  needs: [w1, w2]
  status: done
- id: w4
  summary: "Implement connection factory with singlestoredb SDK (Helios + self-managed)"
  needs: [w3]
  status: done
- id: w5
  summary: "Add columnstore DDL generation for TPC schemas with shard key configuration"
  needs: [w3]
  status: done
- id: w6
  summary: "Implement bulk loading via LOAD DATA LOCAL INFILE"
  needs: [w4, w5]
  status: done
- id: w7
  summary: "Implement query execution and result reading"
  needs: [w4]
  status: done

# --- Credentials ---
- id: w8
  summary: "Add SingleStore credential helper for Helios and self-managed"
  needs: [w3]
  status: done

# --- Testing ---
- id: w9
  summary: "Add unit tests for SingleStore adapter"
  needs: [w4, w5, w6, w7]
  status: done
- id: w10
  summary: "Run TPC-H/TPC-DS validation at SF 0.01 and SF 1"
  needs: [w9]
  status: done

# --- Documentation ---
- id: w11
  summary: "Create platform documentation page and update index/comparison matrix"
  needs: [w4, w5]
  status: done
