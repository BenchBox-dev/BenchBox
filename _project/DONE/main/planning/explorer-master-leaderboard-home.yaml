id: explorer-master-leaderboard-home
title: "Explorer Home: ClickBench-style master leaderboard (Times/Ranks/Speedup, chip filters, URL state)"
worktree: main
priority: High
status: Completed
completed_date: "2026-04-17"
category: Core Functionality

description: |
  Phase B of the Results Explorer strategy revision
  (plans/purring-jingling-petal-ultraplan).

  Replace the current Home page's rank-only `MetaLeaderboard` with a
  ClickBench-style Platform × Cohort matrix of absolute values, with
  toggles for Ranks and Speedup-vs-cohort-best, chip-based filters
  (benchmark family, SF, phase, tuning, trust tier, date window), and
  URL-synced state so views are shareable.

  The "Recent Results" list stays (or moves below the matrix) - the
  gap being filled is the lack of a cross-benchmark, cross-platform
  high-level view with absolute values. This is the first thing a
  user should see.

  Depends on:
    - explorer-pipeline-meta-leaderboard-widening (MetaRank must carry
      metric_value, speedup_vs_best, primary_metric, primary_order)
    - explorer-url-synced-filter-state (useUrlState hook)

work:
  - id: w1
    summary: "Extend MetaLeaderboard component with render-mode toggle (Times/Ranks/Speedup)"
    status: done
    notes: |
      results-explorer/src/components/MetaLeaderboard.tsx - extend in
      place (per plan: data shape already carries what each mode needs
      after the pipeline widening). Modes:
        - Times: metric_value formatted via fmtGeomean (asc) / fmtScore (desc)
        - Ranks: current behavior (rank / total)
        - Speedup: speedup_vs_best, cohort best = 1.0×
      Log-scale color shading via existing colorForCell from
      src/lib/chartMath.ts (single source of truth, shared with
      QueryHeatmap).

  - id: w2
    summary: "Widen MetaRank frontend type in types.ts"
    status: done
    notes: |
      results-explorer/src/types.ts - add metric_value,
      speedup_vs_best, primary_metric, primary_order to MetaRank.
      All optional so the type survives older artifacts.

  - id: w3
    summary: "Sticky filter chip row backed by useUrlState"
    needs: [w1, w2]
    status: done
    notes: |
      Filters: Benchmark family (multi), SF (multi), Phase
      (power/standard/throughput), Tuning mode, Trust tier, Date
      window. Each backed by useUrlState with a suitable serde
      (arraySerde for multis, stringSerde for singles).
      Filter state feeds both the visible cohort columns and the
      visible platform rows.

  - id: w4
    summary: "Scaffold Home.tsx around the extended MetaLeaderboard as primary content"
    needs: [w3]
    status: done
    notes: |
      results-explorer/src/pages/Home.tsx - make the matrix the
      primary content surface. Do not wire cohort/row navigation yet
      (w6 handles that). Do not change the Recent Results position
      yet (w5 handles that). Scope: layout only.

  - id: w5
    summary: "Demote Recent Results list below the matrix"
    needs: [w4]
    status: done
    notes: |
      Keep the Recent Results component and data surface; move its
      render position below the matrix on Home.tsx. No behavioral
      change to the list itself.

  - id: w6
    summary: "Wire cohort header links and row-click navigation with preserved filter state"
    needs: [w4]
    status: done
    notes: |
      Cohort headers → /results/{benchmark}/?sf=X&phase=Y (preserve
      the relevant slice of filter state via useUrlState's serde).
      Row click → /results/p/{platform_id}/.

  - id: w7
    summary: "Accessibility: reuse QueryHeatmap's role=grid + roving tabindex pattern"
    needs: [w4]
    status: done
    notes: |
      The new master matrix is structurally similar to QueryHeatmap -
      reuse the same a11y scaffolding (role=grid, aria-rowindex/col,
      roving tabindex). Existing tests like
      QueryHeatmap.a11y should continue to pass; author a sibling
      MetaLeaderboard.a11y test covering the equivalent contract.

  - id: w8
    summary: "Vitest coverage for render-mode toggle, filter→matrix wiring, cohort-link state preservation"
    needs: [w5, w6, w7]
    status: done

files_affected:
  modified:
    - "results-explorer/src/pages/Home.tsx"
    - "results-explorer/src/components/MetaLeaderboard.tsx"
    - "results-explorer/src/types.ts"
  added:
    - "results-explorer/src/components/__tests__/MetaLeaderboard.test.tsx"
    - "results-explorer/src/components/__tests__/MetaLeaderboard.a11y.test.tsx"

must_preserve:
  - "Recent Results data surface remains accessible on Home (just demoted below the matrix)"
  - "Existing MetaLeaderboard consumers (if any outside Home) continue to work - render-mode toggle defaults to current behavior (Ranks)"
  - "colorForCell remains the single log-scale color source shared with QueryHeatmap - no duplicate palettes"

approach: |
  The pipeline-widening item is the enabling dependency; this item
  is pure frontend wiring on top of the widened MetaRank. Extend in
  place rather than introducing a sibling MasterLeaderboard component
  (per plan) since the data shape already carries everything
  needed. Reuse chartMath.colorForCell and QueryHeatmap's a11y
  pattern - no new primitives. Default render mode on first visit:
  Times, so the user immediately sees absolute values (the missing
  surface this phase exists to restore).

anti_patterns:
  - "DO NOT fork MetaLeaderboard into a separate component - because the shapes are the same post-widening - extend in place"
  - "DO NOT introduce a new color function - because colorForCell is the shared log-scale palette - call it"
  - "DO NOT push filter state to a global store - because per-page useUrlState is sufficient - keep it local"

verification:
  - description: "Explorer typecheck + Vitest suites green"
    command: "cd results-explorer && npm run typecheck && npm test"
    expected_output: "all green, including MetaLeaderboard.a11y and filter wiring tests"
  - description: "End-to-end smoke on /results/"
    command: "cd results-explorer && npm run dev"
    expected_output: "/results/ shows master matrix; Times/Ranks/Speedup toggle works; applying benchmark + SF chips narrows columns/rows; URL round-trips; clicking a cohort header goes to /results/{benchmark}/?sf=X&phase=Y; row click goes to /results/p/{platform_id}/"

deps:
  needs:
    - explorer-pipeline-meta-leaderboard-widening
    - explorer-url-synced-filter-state

scope_limit:
  only_modify:
    - "results-explorer/src/pages/Home.tsx"
    - "results-explorer/src/components/MetaLeaderboard.tsx"
    - "results-explorer/src/components/__tests__/MetaLeaderboard.test.tsx"
    - "results-explorer/src/components/__tests__/MetaLeaderboard.a11y.test.tsx"
    - "results-explorer/src/types.ts"

impact:
  user_impact: |
    Landing page answers "how fast is engine X on cohort Y" at a
    glance with absolute numbers (not just cohort ranks). Users can
    toggle to speedup-vs-best for normalized comparisons, and every
    filter choice is in the URL so views are shareable.
  business_value: |
    Closes the ClickBench-model gap called out in the ultraplan -
    the primary cross-benchmark entry point for prospects evaluating
    BenchBox against alternatives.

metadata:
  tags: ["explorer", "home", "master-leaderboard", "clickbench", "ultraplan-phase-b"]
  estimated_effort: "Medium (1-2 days)"
  created_date: "2026-04-17"
  source: "ultraplan: Results Explorer strategy revision (purring-jingling-petal-ultraplan), Phase B"
