# Trend Chart Verification

Checked branch: `fix/results-explorer-trend-point-identity`.

## Focused Tests

PASS:

- `cd results-explorer && npm test -- --run src/components/__tests__/charts.smoke.test.tsx`
  - 1 file passed, 39 tests passed.
  - Covers point title identity, duplicate-day suppression, duplicate run links, and repeated platform display names.
- `cd results-explorer && npm test -- --run src/pages/__tests__/PlatformIndex.test.tsx`
  - 1 file passed, 29 tests passed.
  - Covers duplicate same-day platform trend state and singular/plural sparse copy.
- `cd results-explorer && npm run build`
  - PASS.
- `cd results-explorer && npm run typecheck`
  - PASS.
- `cd results-explorer && E2E_PORT=4326 npx playwright test e2e/routes/platform-index.spec.ts --project=chromium`
  - 2 tests passed.

## Browser Evidence

Fixture route: `http://127.0.0.1:5186/results/p/duckdb/`.

PASS duplicate-day policy:

- `pointCount`: 0 rendered SVG points in the duplicate-day cohort.
- Duplicate state text: `Trend line hidden: 3 same-day runs in this cohort do not carry time-of-day ordering. 3 observations remain in the cohort; duplicate same-day runs are listed below.`
- Underlying public IDs and links remained reachable:
  - `Public ID 010ee756` -> `/results/r/tpch-duckdb-sf0.01-20260403-010ee756` and `#run-receipt`
  - `Public ID a5eff54e` -> `/results/r/tpch-duckdb-sf0.01-20260403-a5eff54e` and `#run-receipt`
  - `Public ID b6d2e142` -> `/results/r/tpch-duckdb-sf0.01-20260403-b6d2e142` and `#run-receipt`

PASS sparse copy:

- Sparse text rendered `Limited observations: 1 published run in this comparable cohort.`
- `runin` was absent from `document.body.textContent`.

Normal trend route note: the refreshed fixture corpus does not currently contain a platform detail route with at least three non-duplicate dates. Normal line rendering is covered by synthetic component/page tests in this PR.
