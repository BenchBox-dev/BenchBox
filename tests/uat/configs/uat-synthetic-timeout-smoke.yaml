name: "uat-synthetic-timeout-smoke"
description: "Tiny UAT sweep that should time out one local DuckDB TPC-H cell"

phases:
  - preflight
  - enumerate
  - execute
  - report

platforms:
  include: ["duckdb"]

benchmarks:
  include: ["tpch"]

scales:
  rungs: [0.01]

execute:
  per_cell_timeout_s: 1
  early_stop_after_s: 120
  early_stop_on_failure: false
  phases_arg: "load,power"
  skip_unreachable: true
  parallel_platforms: false

preflight:
  free_space_min_gib: 5
  free_space_path: "~/Developer/benchmark_runs"
  docker_required: false

cleanup:
  preserve_datagen: true
  prune_databases: true
  docker_manage_platforms: false
  docker_platform_switch: "off"

report:
  matrix_summary_tsv: "matrix_summary.tsv"

output:
  benchmark_runs_dir_template: "~/Developer/benchmark_runs"
  logs_dir_template: "~/Developer/benchmark_runs/logs/uat_synthetic_timeout_smoke_{date}"
  submissions_dir_template: "~/Developer/benchmark_runs/submissions/{name}"
